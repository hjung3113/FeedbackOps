ROLE: CODEX fixing a BLOCKER in issue #124 (sandboxed, no DB). A read-only REVIEW found it; security (quote redaction) already passed.

THE BLOCKER: `isCreatableTuple()` in apps/backend/src/modules/entity-links/service.ts (~line 242-255) excludes (voc,finding,evidence_of). This function gates BOTH creation AND read-visibility (evaluateRowVisibility ~line 277-284 returns 'hidden' for any tuple not in it) AND detach. POST /findings/:id/link-evidence currently inserts a (voc,finding,evidence_of) row by a DIRECT repo insert, so those links are HIDDEN from GET /entity-links and CANNOT be detached — they are not first-class canonical cross-system history. ADR-0024 §A: entity links are the canonical cross-system layer; evidence_of must be readable + detachable.

REQUIRED FIX:
1. apps/backend/src/modules/entity-links/service.ts: add (voc,finding,evidence_of) to isCreatableTuple so the tuple is a fully-supported relation (creatable + readable + detachable). Confirm evaluateRowVisibility + detach now accept it. The realised read decision for an evidence_of (internal_only) link mirrors the created_finding/finding-target row (ADR-0024 §D): Reporter/User hidden, Dev in-scope (both endpoints readable) allowed, Dev out hidden, Admin allowed.
2. apps/backend/src/modules/findings/service.ts `linkEvidence`: route the link creation THROUGH the entity-links service createLink (registry-based) — source {type:'voc', id:sourceVocId}, target {type:'finding', id:findingId}, relation_type 'evidence_of' — instead of the direct VOC repo lookup + raw insert. This makes it registry-consistent, atomic, and audited via entity_link.created (the evidence_of audit union member already exists). Preserve the existing finding.manage + source-VOC-read authz (createLink already enforces source canRead + target canCreateTarget; verify the finding provider's canCreateTarget = finding.manage on the finding MS, and source canRead = voc.read OR reporter).
3. Tests (extend evidence-highlights.integration.test.ts): after POST /findings/:id/link-evidence, assert the evidence_of link is VISIBLE via GET /entity-links?target_type=finding&target_id=<finding> for an in-scope Developer/Admin (visibility_state allowed) and HIDDEN for Reporter/User; and that it is DETACHABLE via PATCH /entity-links/:id (active->detached). Keep the rate-limit cleanup added in the prior fix.

DO NOT change the migration, the DB tuple CHECK (already includes evidence_of), the source-visibility redaction logic (already correct), or unrelated code. Minimal diff.

When done write .review/ISSUE-124-FIX2.json {"issue":124,"fix":"evidence_of first-class in entity-links (isCreatableTuple + read/detach) + link-evidence via registry createLink","files":[...]}.

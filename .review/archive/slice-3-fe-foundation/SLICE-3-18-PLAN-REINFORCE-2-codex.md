warning: `--full-auto` is deprecated; use `--sandbox workspace-write` instead.
OpenAI Codex v0.131.0
--------
workdir: /Users/hyojung/Desktop/2026/FeedbackOps
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: medium
reasoning summaries: none
session id: 019e40fa-18da-7800-b55d-c7965ec04560
--------
user
Goal: Rewrite `.review/SLICE-3-18-PLAN.md` AND `report/2026-05-20-slice3-18-plan-playground.html` to address every finding in `.review/SLICE-3-18-PLAN-REVIEW-CYCLE-2.md`. This is a plan-document edit only — do NOT touch source code, do NOT install deps, do NOT create branches, do NOT modify ADRs (the plan tells C0 to do that).

Repo root: /Users/hyojung/Desktop/2026/FeedbackOps

## Required reads (in order, before any edit)

1. `.review/SLICE-3-18-PLAN-REVIEW-CYCLE-2.md` (full review, all cycle-2 findings + reinforcement audit)
2. `.review/SLICE-3-18-PLAN.md` (current 10-chunk plan being patched)
3. `.review/SLICE-3-18-PLAN-REVIEW-codex.md` (cycle-1 context, for the P1-4 PARTIAL gap)
4. `docs/frontend/specs/voc.md` (full file — confirm shadcn primitive coverage in §3, especially lines 89, 90, 100, 112, 156-172)
5. `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` (full file — find the "Out of scope → Light-theme support" clause that P0-A flags)
6. `packages/ui/package.json` (current test script no-op per P1-A)
7. `apps/frontend/src/routes/admin/managed-systems.tsx` + `admin/analytics-areas.tsx` + `login.tsx` (token classnames referenced per P1-B — grep for `text-status-*`, `border-surface-*`, `bg-surface-*`)
8. `docs/design-prototype/styles.css` lines 1-200 (full token set — verify P2-A's non-color token list)
9. `apps/frontend/src/routes/__root.tsx` (current root layout per P1-C)

## Required PLAN.md edits — address every finding

Apply all cycle-2 findings. Concrete required changes per finding ID:

### P1-4 residual (shadcn list completion, replaces P0-B)
- Lock shadcn list to **22 primitives**: add `alert`, `hover-card`, `sheet` (shadcn's Drawer equivalent), `combobox` (built from `popover` + `command` parts — note: `command` was deferred per current plan; if Combobox needs Command parts, either include Command for Combobox-only OR scaffold Combobox without Command and accept partial fidelity until a follow-up). Pick one stance and write it.
- If keeping Command deferred: Combobox uses shadcn's "command-less" pattern (search input + popover list filter). Document this trade-off in C1b "Note on `command`".
- Update count line "18 total — locked" to "22 total — locked".

### P0-A (ADR-0020 amendment in C0)
- C0 Files list: add THIRD edit `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` to remove the "Out-of-scope → Light-theme support" clause and add a cross-reference to ADR-0021. Document this as part of C0's commit message: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.

### P1-A (vitest config in packages/ui — must land in C1a step 1)
- C1a Files list: add at the TOP (prerequisite step):
  - Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
  - Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
  - Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
- C1a Dependencies install (new prerequisite block before tokens port):
  ```bash
  pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node
  ```
- C1a constraint addendum: "Step 0: wire vitest in @fops/ui before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED (run a placeholder failing test first to prove the runner picks it up)."

### P1-B (admin route token-class rename audit in C1a)
- C1a Files list: add audit + rename step:
  - Modify: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` (and any other route file referencing token classes — grep first).
  - Audit step before tokens port: `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/` → build rename map → apply.
- Known known renames to document in the plan:
  - `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`
  - `border-surface-overlay` → `border-default` (since Pack 17 has no surface-overlay; popover takes that role)
- Add grep-assertion test to C1a verification: assert no `.tsx` references a class whose Tailwind theme key is not resolvable via `resolveConfig`. Place in `apps/frontend/src/__tests__/token-class-coverage.test.ts` OR in `packages/ui` if it can scan apps/frontend.

### P1-C (_authed.tsx mechanics)
- C5 section: lock the following details explicitly (replace any vagueness):
  1. `apps/frontend/src/routes/_authed.tsx` includes a `beforeLoad` callback that calls `fetchMe()` (existing pattern in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Per-route auth guards in admin pages are DELETED in C5.
  2. File-move list (admin route relocation):
     - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
     - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
     - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
     - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
     - Path strings (`/admin/managed-systems`) UNCHANGED — underscore is pathless.
  3. `apps/frontend/src/routes/login.tsx` stays at current location (sibling of `_authed.tsx`, outside the layout).
  4. Forbid `Route.id` usage anywhere — only `to:` path strings for navigation. Add a lint or grep check.
- Add to "Risks" section: "TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first)."

### P1-D (Button asChild + loading interaction)
- C1b Button.tsx section: lock the chosen contract — **`loading` is incompatible with `asChild`**. When both are true, the wrapper throws in dev (`process.env.NODE_ENV !== 'production'`) and degrades silently in prod (renders the child without loading affordance, logs a warning).
- C1b regression test list MUST include:
  - `<Button loading>` → disabled + aria-busy + spinner ✓
  - `<Button asChild><Link>…</Link></Button>` (no loading) → renders Link, no spinner ✓
  - `<Button asChild loading>` → in dev, throws or logs warning; in prod, renders Link without spinner ✓
- Document the trade-off in a Button.tsx code-comment.

### P1-E (errorMapper test rigor)
- C3 errorMapper.test.ts section: lock the following assertions:
  1. For every code in `ERROR_CODES`: `tone` ∈ `{'error', 'warning', 'info'}` (literal set assertion).
  2. For every code with prefix in `{voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse}`: Korean message is NOT the generic fallback string.
  3. `conflict.stale_write` specifically maps to `{tone: 'warning', action: {label: <not undefined>, run: <fn>}}`.
  4. The fallback message constant (e.g. `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'`) is exported so the test can compare to it directly.

### P2-A (token-fidelity test rigor)
- C1a token-fidelity.test.ts section: replace the simple value-match assertion with:
  1. Parse `tokens.css` with `postcss` or `css-tree` (NOT regex). Add `postcss` to dev deps if not present.
  2. Assert set-equality between parsed token names and fixture token names (both directions — fail on missing AND on extras).
  3. Extend fixture to cover ALL Pack 17 token classes (not just 19 colors): layout (`--sidebar-width`, `--rail-width`, `--toolbar-height`, etc.), spacing scale, radius, shadows, typography sizes, status/severity/confidence semantic tokens. Read full list from `docs/design-prototype/styles.css` lines 1-200.
  4. Assert value match (R G B triple OR raw value for non-color tokens).

### P2-B (sidebar collapse SSR-safety)
- C4b AppSidebar section: lock the read pattern:
  ```ts
  const [collapsed, setCollapsed] = useState(() => {
    if (typeof window === 'undefined') return false;
    return localStorage.getItem('appSidebarCollapsed') === '1';
  });
  ```
- Test: assert first-paint width matches stored value (mock localStorage, mount AppSidebar, check rendered width before any user interaction).

### P2-C (C2/C3 ordering — sanitizer error flow validation)
- Pick option (b): extend Checkpoint 2.5 (C3) to include the editor → sanitizer-fail → toast flow end-to-end. Update C3 playground checkpoint description: "Checkpoint 2.5 playground: Toaster tones + Korean copy for 3+ representative codes + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock." This requires C2's `/dev-rich-editor` route to still exist when C3 lands — confirm C2 doesn't delete it.

### P2-D (DetailPanelSlot ownership)
- C4a + C4b lock: **AppFrame owns the global DetailPanelSlot.** Shells DO NOT own their own detail panel. ListShell exposes a `detailPanel?: ReactNode` prop, but the prop is internally forwarded to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020 line about "ListShell owns optional right detail panel" means ownership of the *content placement intent* (Inbox routes opt into detail), not the *rendering surface*.
- Document this in both C4a (shell prop forwards via hook) and C4b (slot is single source of truth).
- Forbid: multiple registrants — `useDetailPanelSlot` throws if called twice with overlapping lifetimes (use a ref + warning).

### P3-A (lucide-react React 19)
- Risks section: add "lucide-react peer-dep React 19. Bump to >=0.470 or set `pnpm install --strict-peer-dependencies=false` in CI." Note: current frontend has 0.469.0; check latest and update if a 0.470+ exists.

### P3-B (TipTap dedupe verification)
- C2 verification block: add `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm` as a verification step.

### P3-C (Playwright baseline manifest comparison)
- Final §2: replace "screenshot vs prototype `screen-voc.jsx`" with "screenshot vs `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate against the `mustSurvive` text for each route."

### Cross-chunk invariants (top of plan)
- Add: "vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0)."
- Add: "Token-fidelity test covers ALL Pack 17 token classes (colors + layout + spacing + radius + shadow + typography), not just colors (P2-A)."
- Add: "AppFrame is the single DetailPanelSlot owner; shells forward via hook (P2-D)."

### Header updates
- Update Architecture line: still 10 chunks, but note that C1a now has a prerequisite step 0 (vitest wiring).
- Update Locked decisions Q1 line: add "Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography)."
- Update Locked decisions Q2 line: change "18 total — locked" → "22 total — locked (alert, hover-card, sheet, combobox added; command deferred)".

## Required playground edits

Update `report/2026-05-20-slice3-18-plan-playground.html`:
- CHUNKS array reflect the residual reinforcement (C1a now has the vitest + admin-rename prerequisite, C5 has the _authed mechanics).
- Subtitle "10 chunks · 5 checkpoints" stays correct.
- Update Summary blurb to reflect 22 shadcn primitives + ADR-0020 amend in C0.

## Output

1. Overwrite `.review/SLICE-3-18-PLAN.md` with the patched plan.
2. Overwrite `report/2026-05-20-slice3-18-plan-playground.html` (only CHUNKS array + summary blurb; keep all CSS/JS intact).
3. Return a short summary (under 250 words) of what changed.

DO NOT touch source code, ADRs, package.json files (those are C0/C1a/etc. work), or anything outside the two target files above.

hook: SessionStart
hook: SessionStart Completed
hook: UserPromptSubmit
hook: UserPromptSubmit Completed
codex
I’ll patch only the two requested documents. First I’m loading the context-mode tool surface and then I’ll read the required files in order without dumping large raw output into the chat.
mcp: context-mode/ctx_search started
mcp: context-mode/ctx_search (completed)
hook: PostToolUse
hook: PostToolUse Completed
codex
I’m preserving the required read order by indexing each required artifact first, then extracting only the plan sections that need edits. The target is a narrow doc patch, so I’ll avoid touching the implementation files beyond read-only inspection.
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_index started
mcp: context-mode/ctx_index (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_batch_execute started
mcp: context-mode/ctx_batch_execute (completed)
hook: PostToolUse
hook: PostToolUse Completed
codex
The first read batch indexed the route/token checks but returned empty summaries, so I’m rerunning those checks as a single JavaScript analysis step with explicit absolute paths.
mcp: context-mode/ctx_execute started
mcp: context-mode/ctx_execute (completed)
hook: PostToolUse
hook: PostToolUse Completed
mcp: context-mode/ctx_search started
mcp: context-mode/ctx_search (completed)
hook: PostToolUse
hook: PostToolUse Completed
codex
I have the affected sections mapped now. Next I’m taking short line-numbered excerpts around those sections so the patch lands cleanly without broad rewrites.
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'rg -n "CHUNKS|Summary|summary|22|18|ADR-0020|_authed|C1a|C5" report/2026-05-20-slice3-18-plan-playground.html' in /Users/hyojung/Desktop/2026/FeedbackOps
exec
/bin/zsh -lc 'rg -n "''^(#|##|'"\\*\\*Architecture|\\*\\*Locked decisions|\\*\\*Cross-chunk|\\*\\*Files|\\*\\*Verification|\\*\\*CHECKPOINT|\\*\\*Commit|\\*\\*shadcn|\\*\\*Note on|\\*\\*Dependencies|\\*\\*Route mounting|\\*\\*Risks|## Risks|## Final)\" .review/SLICE-3-18-PLAN.md" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
5:<title>Slice 3 #18 — Plan Explorer Playground</title>
8:    --bg:#0b0d10; --panel:#15181d; --panel-2:#0f1216; --bord:#262a31; --bord-2:#1d2025;
11:    --green:#18a86b; --amber:#f5ad42; --red:#e84c4c;
12:    --c0:#6a8dff; --c1:#18a86b; --c1a:#18a86b; --c1b:#2ec483; --c1c:#4ecda0; --c2:#f5ad42; --c3:#00a9e0; --c4:#e84c4c; --c4a:#e84c4c; --c4b:#f47b7b; --c5:#a78bfa; --cf:#3157d5;
20:  .head{padding:18px 20px;border-bottom:1px solid var(--bord)}
23:  .sect{padding:14px 18px;border-bottom:1px solid var(--bord-2)}
42:  .title{margin:0 0 4px;font-size:18px;font-weight:600}
43:  .goal{margin:0 0 18px;color:var(--mute);font-size:13px}
44:  .meta-bar{display:flex;gap:18px;flex-wrap:wrap;margin-bottom:18px;padding:10px 14px;background:var(--panel-2);border:1px solid var(--bord);border-radius:6px;font-size:12px}
51:  .block-body ul{margin:0;padding-left:18px}
61:  .pill.checkpoint{background:rgba(245,173,66,.18);color:#ffd28f}
63:  .pill.sonnet{background:rgba(0,169,224,.15);color:#7dd3fc}
88:      <h1>Slice 3 #18 — Plan Explorer</h1>
120:      <h3>Summary</h3>
122:        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
141:const CHUNKS = [
153:    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
156:    id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
170:    commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
175:    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
177:      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
179:      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
181:      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
190:    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
203:    commit:'feat(slice3 #18): ManagedSystemPicker + AnalyticsAreaPicker on ToggleGroup'
220:    commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
234:    commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
239:    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
251:    commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
268:    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
270:    commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
273:    id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
275:    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
277:      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
278:      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
280:      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
285:    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
286:    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
287:    commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
294:      {type:'create', path:'.review/SLICE-3-18-REVIEW-CYCLE-{1,2}.md'},
295:      {type:'create', path:'.review/SLICE-3-18-PR-BODY.md'},
296:      {type:'create', path:'report/2026-05-XX-slice3-18-final.html  // static ship summary'},
297:      {type:'create', path:'memory/project_slice3_18_pr.md'}
301:    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
314:  ul.innerHTML = CHUNKS.map(c => {
330:    CHUNKS.map((c,i) => {
332:      const arrow = i < CHUNKS.length - 1 ? '<span class="dep-arrow">→</span>' : '';
339:  const c = CHUNKS.find(x => x.id === CURRENT);
356:    <div class="crumb">Slice 3 #18 · feature/18-fe-prologue · develop @ e6577eb</div>
376:  const c = CHUNKS.find(x => x.id === CURRENT);

 succeeded in 0ms:
1:# Slice 3 #18 — FE Prologue Implementation Plan
5:**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
9:**Locked decisions (2026-05-20 user confirm, updated post-codex review):**
19:## Branch + Setup
30:## C0 — ADR-0021 + token-format prep + branch
34:**Files:**
47:**Verification:** `git diff` review only.
51:## C1a — Tokens + Tailwind preset + token-fidelity snapshot test
62:**Files:**
76:**CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
78:**Commit:** `feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot`
82:## C1b — shadcn baseline + Button loading regression fix
86:**shadcn primitive list (18 total — locked):**
89:**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
91:**Dependencies install (packages/ui is owner of shadcn primitives):**
93:# apps/frontend — app-level deps only
96:# packages/ui — shadcn primitive deps live here
107:# packages/ui (C1b pre-installs for C2)
111:# apps/frontend does NOT directly install @tiptap/* — consumed via @fops/ui
114:**Files:**
128:**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
132:## C1c — Picker rebuild + caller migration
136:**Files:**
146:**Commit:** `feat(slice3 #18): ManagedSystemPicker + AnalyticsAreaPicker on ToggleGroup`
150:## C2 — TipTap RichEditor + RichContentRenderer
154:**Note on deps:** TipTap packages were installed in C1b (`@tiptap/core`, `@tiptap/pm`, `@tiptap/html`, `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-link`, `@tiptap/extension-underline`, `@tiptap/extension-placeholder` — all in `packages/ui`). No new installs needed in C2.
156:**Files:**
173:**CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
175:**Commit:** `feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions`
179:## C3 — sonner Toaster + apiClient + errorMapper + idempotency hook
183:**Files:**
199:**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
201:**Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
205:## C4a — Three shells in packages/ui/src/layout/
215:**Files:**
229:**Commit:** `feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020`
233:## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
239:**Files:**
256:**Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
260:## C5 — /vocs route shell + authenticated layout route
264:**Route mounting — LOCKED (no "decide in dispatch" language):**
270:**Files:**
297:**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
299:**Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
303:## Final — adversarial review + push + PR
323:## Cross-chunk invariants
334:## Out of scope (lands in #19/#20/#21)
338:## Risks

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '140,240p' .review/SLICE-3-18-PLAN.md" in /Users/hyojung/Desktop/2026/FeedbackOps
exec
/bin/zsh -lc "sed -n '1,140p' .review/SLICE-3-18-PLAN.md" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
# Slice 3 #18 — FE Prologue Implementation Plan

**Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.

**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).

**Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.

**Locked decisions (2026-05-20 user confirm, updated post-codex review):**
- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
- Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
- Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
- ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
- 2026-05-20: DESIGN.md already updated to Pack 17 light values (this session).

---

## Branch + Setup

```bash
git checkout develop && git pull --ff-only origin develop
git checkout -b feature/18-fe-prologue
```

All commits on `feature/18-fe-prologue`. Push + PR after Final cycle.

---

## C0 — ADR-0021 + token-format prep + branch

**Goal:** Land ADR-0021 (Pack 17 Light System) superseding ADR-0016. Update ADR-0016 frontmatter to `superseded-by: 0021`. Document the R G B vs hex two-format split. No code changes yet.

**Files:**
- Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
- Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.

**Tasks:**
1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.

**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.

**Verification:** `git diff` review only.

---

## C1a — Tokens + Tailwind preset + token-fidelity snapshot test

**Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.

**Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
- `#f3f7fe → 243 247 254` (--color-pitch-black)
- `#fbfdff → 251 253 255` (--color-graphite)
- `#1428a0 → 20 40 160` (--color-neon-lime / --color-aether-blue)

Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.

**Files:**
- Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
- Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
- Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
- Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.

**Dispatch (Sonnet):**
- Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
- Constraint: no shadcn primitives, no Button, no pickers in this chunk.
- Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.

**CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"

**Commit:** `feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot`

---

## C1b — shadcn baseline + Button loading regression fix

**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.

**shadcn primitive list (18 total — locked):**
`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.

**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.

**Dependencies install (packages/ui is owner of shadcn primitives):**
```bash
# apps/frontend — app-level deps only
pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography

# packages/ui — shadcn primitive deps live here
pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
  @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
  @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
  @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
```

**TipTap deps belong in packages/ui (not apps/frontend):**
TipTap React bindings live in `packages/ui` because RichEditor is a shared component. `apps/frontend` gets them transitively via `@fops/ui`.
```bash
# packages/ui (C1b pre-installs for C2)
pnpm --filter @fops/ui add @tiptap/core @tiptap/pm @tiptap/html @tiptap/react \
  @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline \
  @tiptap/extension-placeholder
# apps/frontend does NOT directly install @tiptap/* — consumed via @fops/ui
```

**Files:**
- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
- Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.

**Dispatch (Sonnet):**
- Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
- Constraint: no TipTap, no pickers, no AppShell, no route changes.
- Constraint: failing-first for Button loading test — write test before rewrite.
- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.

**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`

---

## C1c — Picker rebuild + caller migration

**Goal:** `ManagedSystemPicker` and `AnalyticsAreaPicker` rebuilt on shadcn ToggleGroup. Existing picker test passes. 1 caller migrated.

**Files:**
- Rewrite: `packages/ui/src/components/ManagedSystemPicker.tsx` — replace native `<select>` with shadcn ToggleGroup (chip style per spec §3.4) keeping `PickerOption[]` + `onChange(value: string|null)` dumb-prop contract.
- Rewrite: `packages/ui/src/components/AnalyticsAreaPicker.tsx` — same pattern.
- (existing) `apps/frontend/src/components-test-pickers.test.tsx` — must pass unchanged.


 succeeded in 0ms:

**Dispatch (Sonnet):**
- Read first: `packages/ui/src/components/{ManagedSystemPicker,AnalyticsAreaPicker}.tsx`, `apps/frontend/src/components-test-pickers.test.tsx`, `docs/frontend/specs/voc.md` §3.4.
- Constraint: dumb-prop contract (`PickerOption[]`, `onChange(string|null)`) must survive intact.
- Verification: `pnpm test` (picker test must still pass).

**Commit:** `feat(slice3 #18): ManagedSystemPicker + AnalyticsAreaPicker on ToggleGroup`

---

## C2 — TipTap RichEditor + RichContentRenderer

**Goal:** `<RichEditor>` + `<RichContentRenderer>` + 2 custom extensions land in `packages/ui/src/rich-content/`. Demo route renders editor in `voc-description` surface. Renderer mode strips mentions for reporter_visible.

**Note on deps:** TipTap packages were installed in C1b (`@tiptap/core`, `@tiptap/pm`, `@tiptap/html`, `@tiptap/react`, `@tiptap/starter-kit`, `@tiptap/extension-link`, `@tiptap/extension-underline`, `@tiptap/extension-placeholder` — all in `packages/ui`). No new installs needed in C2.

**Files:**
- Create: `packages/ui/src/rich-content/extensions/attachmentRef.ts` — TipTap Node `{ name: 'attachmentRef', group: 'block', attrs: { id, name, sizeBytes, mimeType }, parseHTML/renderHTML/addNodeView }`.
- Create: `packages/ui/src/rich-content/extensions/mention.ts` — TipTap Node `{ name: 'mention', group: 'inline', inline: true, attrs: { actor_id, label } }`.
- Create: `packages/ui/src/rich-content/RichEditor.tsx` — props `{ surface: string, value?, defaultValue?, onChange, placeholder?, disabled?, minHeight?, toolbar?: ReactNode }`. Extensions: `StarterKit.configure({ image: false })` + Link + Underline + Placeholder + attachmentRef + mention. Returns editor instance via render-prop for toolbar.
- Create: `packages/ui/src/rich-content/RichContentRenderer.tsx` — props `{ doc: TipTapDoc, mode: 'reporter_visible'|'internal' }`. Uses `generateHTML` from `@tiptap/html` with same extension set. `reporter_visible` strips mention nodes pre-render.
- Modify: `packages/ui/src/index.ts` — export RichEditor, RichContentRenderer, attachmentRef, mention, types.
- Create: `apps/frontend/src/routes/dev-rich-editor.tsx` — temporary demo route (keep behind `import.meta.env.DEV` guard or delete in Final). Renders RichEditor + RichContentRenderer side by side, with surface picker + mode toggle.
- Create: `packages/ui/__tests__/rich-content.test.tsx` — controlled value round-trips through onChange; reporter_visible strips mentions; internal preserves; attachmentRef + mention attrs round-trip TipTap doc → HTML → TipTap doc.

**Dispatch (Sonnet):**
- Read first: `docs/design-prototype/rich-editor.jsx` (toolbar baseline), `docs/adr/0011-*.md`, `apps/backend/src/lib/rich-content/sanitizer.ts` (BE wire shape).
- Constraint: surface allowlist NOT enforced client-side — opaque pass-through to feature-supplied toolbar config.
- Constraint: editor must NOT register Image extension (StarterKit image: false).
- Constraint: no API calls. Demo route uses local state only.
- Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.

**CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"

**Commit:** `feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions`

---

## C3 — sonner Toaster + apiClient + errorMapper + idempotency hook

**Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.

**Files:**
- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
- Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
- Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
- Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
- Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
- Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.

**Dispatch (Sonnet):**
- Read first: `packages/shared/src/errors/codes.ts` (full `ERROR_CODES` array — iterate it), `apps/backend/src/lib/errors/envelope.ts`, `apps/backend/src/modules/voc/routes.ts` lines 200-260 (If-Match + idempotency hash).
- Constraint: Korean copy only. No i18n framework wiring (deferred).
- Constraint: apiClient must not import from any feature folder. Lives in `lib/api/` only.
- Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
- Verification: `pnpm --filter @fops/frontend typecheck && test`.

**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"

**Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`

---

## C4a — Three shells in packages/ui/src/layout/

**Goal:** `PageShell`, `ListShell`, `WorkbenchShell` + shared `ShellHeader` primitive land in `packages/ui/src/layout/`. Each shell conforms to ADR-0020 §3 slot contract. 50px header rhythm enforced. Re-exported from `packages/ui/src/index.ts`.

**ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
- `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
- `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.

**Files:**
- Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
- Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
- Create: `packages/ui/src/layout/WorkbenchShell.tsx` — per ADR-0020 §1.
- Create: `packages/ui/src/layout/ShellHeader.tsx` — shared 50px header primitive. Height derived from `var(--toolbar-height)` token (50px), NOT hardcoded pixel constant.
- Modify: `packages/ui/src/index.ts` — re-export PageShell, ListShell, WorkbenchShell, ShellHeader.
- Create: `packages/ui/__tests__/shell-taxonomy.test.tsx` — 3 tests: (a) each of the 3 shells renders without throw; (b) ShellHeader height = 50px (via rendered style / token); (c) slot contracts smoke — PageShell renders `actions` slot, ListShell renders `toolbar` + body slots, WorkbenchShell renders `toolbar` + body slots.

**Dispatch (Sonnet):**
- Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
- Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
- Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.

**Commit:** `feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020`

---

## C4b — AppFrame in apps/frontend + useFullscreenPanel hook

**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.

**AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.

**Files:**
- Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.

hook: PostToolUse
hook: PostToolUse
hook: PreToolUse
hook: PreToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "sed -n '240,360p' .review/SLICE-3-18-PLAN.md" in /Users/hyojung/Desktop/2026/FeedbackOps
exec
/bin/zsh -lc "sed -n '110,310p' report/2026-05-20-slice3-18-plan-playground.html" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
- Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
- Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
- Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
- Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).

**Dispatch (Sonnet):**
- Read first: `docs/design-prototype/app.jsx`, `docs/adr/0020-*.md`, `docs/frontend/specs/voc.md` §6.7, `packages/ui/src/layout/` (shells just created in C4a).
- Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
- Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
- Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
- Verification: typecheck + test.

**Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`

---

## C5 — /vocs route shell + authenticated layout route

**Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.

**Route mounting — LOCKED (no "decide in dispatch" language):**
- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
- `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).

**Files:**
- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
- Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
- Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
- Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
- Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).

**Search schema:**
```ts
const vocSearchSchema = z.object({
  view: z.enum(['inbox', 'my', 'triage']).optional(),
  action: z.enum(['create']).optional(),
  selected: z.string().uuid().optional(),
  managedSystem: z.string().optional(),
  tab: z.string().optional(),
  sort: z.string().optional(),
}).passthrough(); // filter.* parsed per-view in #20
```

**Dispatch (Sonnet):**
- Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
- Constraint: zero feature logic in /vocs — placeholder only.
- Constraint: login + non-authed routes must NOT render AppFrame — test this.
- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.

**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"

**Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`

---

## Final — adversarial review + push + PR

**Goal:** 2 cycles adversarial review (codex CLI → Opus subagent), reinforce gaps, push branch, open PR to develop, squash-merge after user OK, sync memory + wiki.

**Tasks:**
1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
6. Reinforce gaps via Sonnet.
7. CHECKPOINT 4 (Final): static HTML report — ship summary, follow-ups table, diff stats, test counts. AskUserQuestion: "최종 OK?"
8. `git push -u origin feature/18-fe-prologue` (agent allowed per orchestration §1).
9. `gh pr create --base develop --title "feat(slice3 #18): FE prologue — shadcn + TipTap + Pack 17 tokens + AppFrame + shells + /vocs shell" --body @.review/SLICE-3-18-PR-BODY.md`.
10. After user merge OK: `gh pr merge --squash --delete-branch`. Do NOT close issue (orchestration §2).
11. Sync memory: write `project_slice3_18_pr.md`, update MEMORY.md.
12. Sync llmwiki: append #18 PR commit to relevant pages (rich-content-sanitizer, shell-taxonomy, bounded-context-voc).

---

## Cross-chunk invariants

- Every chunk ends with `pnpm typecheck` green.
- No chunk touches files outside its declared file set without re-dispatch.
- Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
- Tests added per chunk run live, not deferred.
- **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
- **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
- `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
- Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).

## Out of scope (lands in #19/#20/#21)

VOC list / row / toolbar / filter / sort, DetailPanel components, badge primitives, EntityHoverPreview, ComposerTabs, triage queue, severity picker, real attachment UI, mobile layout, AppRail content, CommandMenu wiring (cmdk installed only — `command` shadcn primitive deferred to a separate FE issue).

## Risks

- Pack 17 token swap may break Slice 1/2 admin pages visually (admin uses semantic tokens → should auto-light-render but verify in C1a checkpoint).
- TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
- TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
- **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
- `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
- Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.

 succeeded in 0ms:
        <label class="toggle on"><input type="checkbox" data-key="goal" checked> Goal</label>
        <label class="toggle on"><input type="checkbox" data-key="files" checked> Files</label>
        <label class="toggle on"><input type="checkbox" data-key="deps" checked> Dependencies (install)</label>
        <label class="toggle on"><input type="checkbox" data-key="dispatch" checked> Dispatch brief</label>
        <label class="toggle on"><input type="checkbox" data-key="verify" checked> Verification</label>
        <label class="toggle on"><input type="checkbox" data-key="checkpoint" checked> Checkpoint + commit</label>
      </div>
    </div>

    <div class="sect">
      <h3>Summary</h3>
      <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
        Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
      </div>
    </div>
  </aside>

  <main class="right">
    <div class="preview" id="preview"></div>
    <div class="bottom">
      <h4>Generated Prompt (copy to Claude main session)</h4>
      <div class="prompt-wrap">
        <div class="prompt-box" id="prompt"></div>
        <button class="copy" id="copyBtn">Copy</button>
      </div>
    </div>
  </main>
</div>

<script>
const CHUNKS = [
  {
    id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
    risk:'low', loc:'~80',
    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
    files:[
      {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
    ],
    install:null,
    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
    verify:'git diff review only',
    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
  },
  {
    id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
    risk:'high', loc:'~300',
    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
    files:[
      {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
      {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
      {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
      {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
    ],
    install:null,
    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
    commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
  },
  {
    id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
    risk:'high', loc:'~600',
    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
    files:[
      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
      {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
    ],
    install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
# apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
  },
  {
    id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
    risk:'low', loc:'~250',
    goal:'ManagedSystemPicker + AnalyticsAreaPicker on shadcn ToggleGroup. Dumb-prop contract preserved. Existing picker test passes unchanged.',
    files:[
      {type:'rewrite', path:'packages/ui/src/components/ManagedSystemPicker.tsx  // ToggleGroup chip-style, PickerOption[]+onChange(string|null)'},
      {type:'rewrite', path:'packages/ui/src/components/AnalyticsAreaPicker.tsx  // same pattern'}
    ],
    install:'(none)',
    dispatch:'Sonnet. Read ManagedSystemPicker.tsx + AnalyticsAreaPicker.tsx + components-test-pickers.test.tsx + spec voc.md §3.4. Constraint: dumb-prop contract (PickerOption[], onChange(string|null)) must survive intact.',
    verify:'pnpm test  // existing picker test must pass unchanged',
    commit:'feat(slice3 #18): ManagedSystemPicker + AnalyticsAreaPicker on ToggleGroup'
  },
  {
    id:'c2', name:'C2 — TipTap RichEditor + Renderer', dot:'var(--c2)', model:'sonnet', checkpoint:'CHECKPOINT 2',
    risk:'med', loc:'~600',
    goal:'<RichEditor> + <RichContentRenderer> + attachmentRef + mention. Demo route on dev-only. reporter_visible strips mentions. No @tiptap/* in apps/frontend.',
    files:[
      {type:'create', path:'packages/ui/src/rich-content/RichEditor.tsx'},
      {type:'create', path:'packages/ui/src/rich-content/RichContentRenderer.tsx'},
      {type:'create', path:'packages/ui/src/rich-content/extensions/{attachmentRef,mention}.ts'},
      {type:'modify', path:'packages/ui/src/index.ts  // export rich-content'},
      {type:'create', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // DEV-only demo'},
      {type:'create', path:'packages/ui/__tests__/rich-content.test.tsx'}
    ],
    install:'(none — TipTap pre-installed in C1b in packages/ui)',
    dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
    commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
  },
  {
    id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
    risk:'med', loc:'~500',
    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
    files:[
      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
      {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
      {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
    ],
    install:'(none — sonner installed in C1b)',
    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
    verify:'pnpm --filter @fops/frontend typecheck && test',
    commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
  },
  {
    id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
    risk:'med', loc:'~400',
    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
    files:[
      {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
      {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
      {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
      {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
    ],
    install:'(none)',
    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
    commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
  },
  {
    id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
    risk:'med', loc:'~500',
    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
    files:[
      {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
      {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
      {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
      {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
      {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
    ],
    install:'(none)',
    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
    verify:'typecheck + test',
    commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
  },
  {
    id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
    risk:'med', loc:'~300',
    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
    files:[
      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
      {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
      {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
    ],
    install:'(none)',
    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
    commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
  },
  {
    id:'cf', name:'Final — review + push + PR', dot:'var(--cf)', model:'opus', checkpoint:'CHECKPOINT 4',
    risk:'low', loc:'—',
    goal:'2 cycle adversarial review (codex + Opus), reinforce gaps, Playwright screenshots, push, PR, squash-merge, sync memory + wiki.',
    files:[
      {type:'create', path:'.review/SLICE-3-18-REVIEW-CYCLE-{1,2}.md'},
      {type:'create', path:'.review/SLICE-3-18-PR-BODY.md'},
      {type:'create', path:'report/2026-05-XX-slice3-18-final.html  // static ship summary'},
      {type:'create', path:'memory/project_slice3_18_pr.md'}
    ],
    install:'(none)',
    dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
    commit:'(squash-merge on develop)'
  }
];

const TOGGLES = { goal:true, files:true, deps:true, dispatch:true, verify:true, checkpoint:true };
let CURRENT = 'c1';
let PRESET = 'overview';

function escapeHtml(s){ return s.replace(/[&<>]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;'}[c])); }

hook: PostToolUse
hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
web search: 
web search: npm lucide-react 0.470.0 React 19 peerDependencies
codex
I’m going to apply the plan changes now: first the markdown plan, then the playground’s summary and CHUNKS data only. No source, ADR, or package files will be modified.
hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/.review/SLICE-3-18-PLAN.md
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..5dc267a92f374d61cd597d8bd8064ccdb35b6e44
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..5dc267a92f374d61cd597d8bd8064ccdb35b6e44
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 

hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/.review/SLICE-3-18-PLAN.md
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..83e92e838b102174e06e9e080381aba61c382c77
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..83e92e838b102174e06e9e080381aba61c382c77
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`

hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/.review/SLICE-3-18-PLAN.md
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc "rg -n \"18 shadcn|18 total|re-export 18|each of the 18|/_authed/vocs|screen-voc|surface-overlay|no tests yet|Route\\.id|command primitive deferred|C1a now|22 total|DetailPanelSlot|Vitest|token-fidelity\" .review/SLICE-3-18-PLAN.md" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
5:**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
11:- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
41:2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
53:## C1a — Tokens + Tailwind preset + token-fidelity snapshot test
57:**Step 0 — Vitest prerequisite (must land before any C1a test):**
63:- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
64:- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
76:- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
80:- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
81:- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
91:  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
92:- Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
97:**Commit:** `feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot`
105:**shadcn primitive list (22 total — locked):**
235:**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
248:- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
258:**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
263:- Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
273:- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
274:- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
276:- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
284:- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
287:**Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
305:- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
306:- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
310:- Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
337:- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
372:- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
376:- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
378:- Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
390:- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.

hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/report/2026-05-20-slice3-18-plan-playground.html
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..a249b0723bf5e598df7e23d5b43044b9b056b94c
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,31 +142,37 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..a249b0723bf5e598df7e23d5b43044b9b056b94c
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,31 +142,37 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {

hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/report/2026-05-20-slice3-18-plan-playground.html
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..7ab2bed1cbc07b4fbbbb11d3076e54caa09137ce
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,20 +222,20 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..7ab2bed1cbc07b4fbbbb11d3076e54caa09137ce
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,20 +222,20 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },

hook: PreToolUse
hook: PreToolUse Completed
apply patch
patch: completed
/Users/hyojung/Desktop/2026/FeedbackOps/report/2026-05-20-slice3-18-plan-playground.html
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

codex
The requested patches are in place. I’m doing a narrow verification now: checking that only the two target files changed and scanning for stale “18 shadcn” or old `/ _authed` URL wording.
hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc 'git diff --name-only' in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
DESIGN.md

hook: PostToolUse
exec
/bin/zsh -lc "rg -n \"18 shadcn|18 total|re-export 18|each of 18|/_authed/vocs|screen-voc\\.jsx|prototype screenshots|ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'"'$|no-op test|surface-overlay;|C1a now has|22 shadcn|22 total|beforeLoad|Route'"\\.id|manifest\\.json|mustSurvive\" .review/SLICE-3-18-PLAN.md report/2026-05-20-slice3-18-plan-playground.html" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
report/2026-05-20-slice3-18-plan-playground.html:122:        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
report/2026-05-20-slice3-18-plan-playground.html:181:    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
report/2026-05-20-slice3-18-plan-playground.html:183:      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
report/2026-05-20-slice3-18-plan-playground.html:196:    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
report/2026-05-20-slice3-18-plan-playground.html:281:    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
report/2026-05-20-slice3-18-plan-playground.html:283:      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
report/2026-05-20-slice3-18-plan-playground.html:284:      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
report/2026-05-20-slice3-18-plan-playground.html:288:      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
report/2026-05-20-slice3-18-plan-playground.html:292:    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
report/2026-05-20-slice3-18-plan-playground.html:293:    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
report/2026-05-20-slice3-18-plan-playground.html:308:    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
.review/SLICE-3-18-PLAN.md:11:- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
.review/SLICE-3-18-PLAN.md:76:- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
.review/SLICE-3-18-PLAN.md:103:**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
.review/SLICE-3-18-PLAN.md:105:**shadcn primitive list (22 total — locked):**
.review/SLICE-3-18-PLAN.md:134:- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
.review/SLICE-3-18-PLAN.md:136:- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
.review/SLICE-3-18-PLAN.md:147:**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
.review/SLICE-3-18-PLAN.md:296:- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
.review/SLICE-3-18-PLAN.md:297:- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
.review/SLICE-3-18-PLAN.md:305:- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
.review/SLICE-3-18-PLAN.md:306:- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
.review/SLICE-3-18-PLAN.md:309:- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
.review/SLICE-3-18-PLAN.md:310:- Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
.review/SLICE-3-18-PLAN.md:316:- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
.review/SLICE-3-18-PLAN.md:337:- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
.review/SLICE-3-18-PLAN.md:340:**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
.review/SLICE-3-18-PLAN.md:352:2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
.review/SLICE-3-18-PLAN.md:390:- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**

hook: PostToolUse
hook: PreToolUse
hook: PostToolUse Completed
hook: PreToolUse Completed
hook: PostToolUse Completed
exec
/bin/zsh -lc 'git diff --stat -- .review/SLICE-3-18-PLAN.md report/2026-05-20-slice3-18-plan-playground.html' in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc 'git status --short -- .review/SLICE-3-18-PLAN.md report/2026-05-20-slice3-18-plan-playground.html DESIGN.md' in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
 M DESIGN.md
?? .review/SLICE-3-18-PLAN.md

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

hook: PreToolUse
hook: PreToolUse Completed
exec
/bin/zsh -lc 'git ls-files --error-unmatch report/2026-05-20-slice3-18-plan-playground.html .review/SLICE-3-18-PLAN.md 2>/dev/null; git status --short --untracked-files=all -- report/2026-05-20-slice3-18-plan-playground.html .review/SLICE-3-18-PLAN.md' in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
?? .review/SLICE-3-18-PLAN.md

hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

hook: PreToolUse
hook: PreToolUse
hook: PreToolUse Completed
hook: PreToolUse Completed
exec
/bin/zsh -lc "stat -f '%N %Sm %z' .review/SLICE-3-18-PLAN.md report/2026-05-20-slice3-18-plan-playground.html" in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
.review/SLICE-3-18-PLAN.md May 20 01:07:48 2026 40944
report/2026-05-20-slice3-18-plan-playground.html May 20 01:09:23 2026 29851

hook: PostToolUse
exec
/bin/zsh -lc 'git check-ignore -v report/2026-05-20-slice3-18-plan-playground.html .review/SLICE-3-18-PLAN.md || true' in /Users/hyojung/Desktop/2026/FeedbackOps
 succeeded in 0ms:
.gitignore:19:report/	report/2026-05-20-slice3-18-plan-playground.html

hook: PostToolUse
hook: PostToolUse Completed
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

mcp: context-mode/ctx_execute started
mcp: context-mode/ctx_execute (completed)
hook: PostToolUse
hook: PostToolUse Completed
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

codex
Updated the two requested files:

- [.review/SLICE-3-18-PLAN.md](/Users/hyojung/Desktop/2026/FeedbackOps/.review/SLICE-3-18-PLAN.md)
- [report/2026-05-20-slice3-18-plan-playground.html](/Users/hyojung/Desktop/2026/FeedbackOps/report/2026-05-20-slice3-18-plan-playground.html)

Covered all cycle-2 findings: 22 shadcn primitives, C0 ADR-0020 amendment, C1a Vitest prerequisite and token-class audit, stricter token-fidelity/errorMapper tests, `_authed` pathless auth mechanics, Button `asChild + loading` contract, SSR-safe sidebar collapse, single AppFrame DetailPanelSlot ownership, C3 sanitizer toast flow, TipTap dedupe check, and manifest-based Playwright baseline validation.

I also updated the playground CHUNKS array and summary blurb. Verification scan passed for the requested markers, and the playground CHUNKS array still parses as 10 chunks. I did not edit source code, ADRs, package files, or install dependencies. Note: `DESIGN.md` was already dirty in the worktree; I did not touch it.

Source used for the lucide risk note: npm package listing for `lucide-react` showing `0.470.0+` availability: https://www.npmjs.com/package/lucide-react
diff --git a/.review/SLICE-3-18-PLAN.md b/.review/SLICE-3-18-PLAN.md
index f3d6ebbd70533256e35e84396d3b35f541325970..8cca8bedc71b248831ea19f3fbab01a07a229428
--- a/.review/SLICE-3-18-PLAN.md
+++ b/.review/SLICE-3-18-PLAN.md
@@ -2,13 +2,13 @@
 
 **Goal:** Land shadcn baseline + TipTap RichEditor + sonner + Pack-17 light tokens + three route shells (PageShell/ListShell/WorkbenchShell per ADR-0020) + AppFrame + `/vocs` route shell so #19/#20/#21 can build VOC surfaces.
 
-**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
+**Architecture:** 10 chunks (C0, C1a, C1b, C1c, C2, C3, C4a, C4b, C5, Final) dispatched sequentially. C1a now starts with prerequisite step 0 (wire Vitest in `@fops/ui`) before any token test work. Each chunk = one Sonnet subagent dispatch with single Goal + narrow file set. Main session orchestrates, integrates between chunks, runs typecheck, dispatches next. 5 checkpoints between chunks (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 
 **Tech Stack:** React 19, TanStack Router 1.170, TipTap 2, shadcn/ui + Radix, Tailwind 3 (NOT v4 — `tailwind.config.ts` stays v3 API), sonner, cmdk, Zod, react-hook-form, lucide-react. CVA + tailwind-merge for variants.
 
 **Locked decisions (2026-05-20 user confirm, updated post-codex review):**
-- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split.
-- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`.
+- Q1=A (updated): Pack 17 (Samsung-light) verbatim port. ADR-0016 supersede via new ADR-0021. **R G B triple format** (NOT hex). Tailwind preset uses `rgb(var(--color-X) / <alpha-value>)` syntax to preserve `/15` opacity utilities like `bg-severity-high/15`. DESIGN.md keeps hex notation for human readability. ADR-0021 documents the two-format split. Token fidelity test covers full Pack 17 token surface (colors + layout + spacing + radius + shadows + typography).
+- Q2=A: shadcn at `packages/ui/src/components/shadcn/`, re-exported from `packages/ui/src/index.ts`. 22 total — locked (`alert`, `hover-card`, `sheet`, `combobox` added; `command` deferred).
 - Q3=A: Existing `Button.tsx` replaced by shadcn CVA Button. variant primary→default alias. 1 caller migrated. `loading` prop PRESERVED (see P1-3 fix).
 - Q4=A (updated): 10 chunks + 5 checkpoints (after C1a, C2, C3 toast quick check, C4b+C5 merged, Final).
 - ADR-0020 taxonomy enforced: `packages/ui/src/layout/` exports exactly 3 shells (PageShell, ListShell, WorkbenchShell). AppFrame in `apps/frontend/src/lib/layout/` is NOT a 4th shell.
@@ -34,15 +34,17 @@
 **Files:**
 - Create: `docs/adr/0021-pack-17-samsung-light-design-system.md`
 - Modify: `docs/adr/0016-frontend-color-tokens.md` (or whichever ADR locks dark palette — locate first) — add supersede marker.
+- Modify: `docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md` — remove the "Out of scope → Light-theme support" clause and add a cross-reference to ADR-0021.
 
 **Tasks:**
 1. Locate existing dark-palette ADR (likely 0016). `ls docs/adr/ | grep -i color\|token\|design`.
 2. Write ADR-0021 with: context (Pack 17 prototype refresh + Samsung-blue accent), decision (verbatim port from `docs/design-prototype/styles.css` to `packages/ui/src/styles/tokens.css` + `semantic.css`; **R G B triple format** in runtime tokens.css for Tailwind `<alpha-value>` composition; hex notation preserved in DESIGN.md and in `token-fidelity.fixture.ts` snapshot for human readability), consequences (Slice 1/2 surfaces auto re-render via tokens, Button variants remapped, focus ring tinted Samsung-blue, `/15` opacity utilities like `bg-severity-high/15` work because tokens use R G B triples).
 3. ADR-0021 explicitly calls out the two-format rule: DESIGN.md / fixture = hex; runtime tokens.css = R G B triple.
 4. Add `supersedes: 0016` to ADR-0021 frontmatter and `superseded-by: 0021` to ADR-0016.
-5. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016`.
+5. Amend ADR-0020: remove "Out of scope → Light-theme support" because ADR-0021 now owns light-theme support; add "See ADR-0021 for Pack 17 Samsung-light tokens."
+6. Commit: `docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope`.
 
-**Dispatch:** Haiku subagent (doc-only). 1 file create + 1 file edit.
+**Dispatch:** Haiku subagent (doc-only). 1 file create + 2 file edits.
 
 **Verification:** `git diff` review only.
 
@@ -52,6 +54,15 @@
 
 **Goal:** Pack 17 raw tokens live in `packages/ui/src/styles/tokens.css` (R G B triple format), semantic tokens re-derived in `semantic.css`, Tailwind preset exposes them with `<alpha-value>` composition. Token-fidelity snapshot test frozen against `docs/design-prototype/styles.css`.
 
+**Step 0 — Vitest prerequisite (must land before any C1a test):**
+```bash
+pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss
+```
+- Create: `packages/ui/vitest.config.ts` with `environment: 'jsdom'` and `globals: true`.
+- Create: `packages/ui/vitest.setup.ts` importing `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace `"test": "echo 'no tests yet' && exit 0"` with `"test": "vitest run"`, add `"test:watch": "vitest"`.
+- Constraint addendum: wire Vitest in `@fops/ui` before writing any other test file. Verify `pnpm --filter @fops/ui test` exits non-zero on RED by running a placeholder failing test first to prove the runner picks it up.
+
 **Conversion rule:** Each Pack 17 hex from `docs/design-prototype/styles.css` is converted to its R G B triple in `tokens.css`. Rule: `#RRGGBB → R G B` (decimal, space-separated). Examples:
 - `#f3f7fe → 243 247 254` (--color-pitch-black)
 - `#fbfdff → 251 253 255` (--color-graphite)
@@ -60,18 +71,26 @@
 Do not enumerate all 19 colors inline — store the full mapping in the fixture (see below) and reference `docs/design-prototype/styles.css` as the single source of truth.
 
 **Files:**
+- Create: `packages/ui/vitest.config.ts` — jsdom + globals + React plugin.
+- Create: `packages/ui/vitest.setup.ts` — imports `@testing-library/jest-dom`.
+- Modify: `packages/ui/package.json` — replace no-op test script with Vitest scripts.
 - Rewrite: `packages/ui/src/styles/tokens.css` — verbatim port from `docs/design-prototype/styles.css` (~140 lines, all `--color-*` + layout + spacing + radius + shadow + typography vars). **R G B triple format for all color tokens** so Tailwind `rgb(var(--color-X) / <alpha-value>)` works.
 - Rewrite: `packages/ui/src/styles/semantic.css` — re-derive semantic tokens against new raw colors; **remap shadcn HSL CSS vars to R G B triple references** (shadcn HSL vars must NOT inherit defaults — semantic.css rewrites `--background`, `--foreground`, `--primary`, etc. to point at the new R G B vars).
 - Rewrite: `packages/ui/tailwind.preset.ts` — kebab-case keys (`surface-canvas`, `text-primary`, `severity-high`, etc.) mapped to `rgb(var(--…) / <alpha-value>)`. Add spacing + radius + boxShadow extends. **Tailwind 3 syntax only** (no `@theme` v4 blocks).
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex, rgb }` for all 19 `--color-*` tokens. Fixture is the source of truth for snapshot test.
-- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — reads `tokens.css`, parses R G B values, asserts each matches the fixture's `rgb` field. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.fixture.ts` — CSV/JSON map of `{ tokenName, hex?, rgb?, raw? }` for ALL Pack 17 token classes from `docs/design-prototype/styles.css` lines 1-200: colors, surfaces, borders, focus, icon sizes, layout (`--sidebar-width`, `--rail-width`, `--detail-panel-width`, `--toolbar-height`, etc.), spacing scale (`--spacing-*`), radius, shadows, typography sizes/leading/tracking, status/reporter/internal, severity, confidence semantic tokens.
+- Create (failing-first RED): `packages/ui/src/styles/__tests__/token-fidelity.test.ts` — parse `tokens.css` with `postcss` (NOT regex), assert set-equality between parsed token names and fixture token names in both directions (fail on missing and extras), then assert value match: R G B triple for color tokens, raw value for non-color tokens. Also asserts every semantic Tailwind key (surface, text, border, status, severity, density, layout) has a corresponding Tailwind theme key resolvable via `resolveConfig`. Test must run RED before the port, then GREEN after.
+- Modify/audit: `apps/frontend/src/routes/admin/managed-systems.tsx`, `apps/frontend/src/routes/admin/analytics-areas.tsx`, `apps/frontend/src/routes/login.tsx`, `apps/frontend/src/routes/index.tsx` and any other route file referencing token classes — grep first, build rename map, then apply.
+- Create: `apps/frontend/src/__tests__/token-class-coverage.test.ts` — grep/scan `.tsx` class strings and assert no `text|border|bg|ring|fill|stroke` token class references a Tailwind theme key that is not resolvable via `resolveConfig`.
 - Modify: `apps/frontend/src/styles.css` — keep `@import "@fops/ui/styles/tokens.css"` + `semantic.css` + tailwind directives.
 
 **Dispatch (Sonnet):**
 - Read first: `docs/design-prototype/styles.css`, `packages/ui/src/styles/{tokens,semantic}.css`, `packages/ui/tailwind.preset.ts`.
 - Constraint: no shadcn primitives, no Button, no pickers in this chunk.
+- Constraint: audit existing route token classes before token port:
+  `grep -rn '\(text\|border\|bg\|ring\|fill\|stroke\)-\(status\|surface\|accent\|severity\|reporter\|internal\|confidence\|focus\)-' apps/frontend/src/`
+  Known renames: `text-status-danger` → `text-accent-danger` (existing) or new `text-danger`; `border-surface-overlay` → `border-default` because Pack 17 has no `surface-overlay` token and popover owns overlay surfaces.
 - Constraint: failing-first discipline — write token-fidelity.test.ts BEFORE porting tokens, run RED, then port.
-- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
+- Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`. Also run the frontend token-class coverage test and confirm no `.tsx` references a class whose Tailwind theme key is unresolved.
 
 **CHECKPOINT 1** — playground HTML: prototype token table vs tokens.css R G B triples side-by-side, Tailwind `/15` opacity swatch matrix. AskUserQuestion: "토큰 R G B 변환 OK? opacity utility 작동 확인?"
 
@@ -81,12 +100,12 @@
 
 ## C1b — shadcn baseline + Button loading regression fix
 
-**Goal:** 18 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
+**Goal:** 22 shadcn primitives in `packages/ui/src/components/shadcn/`, Button wholesale-replaced with shadcn CVA Button preserving `loading` prop and `aria-busy` contract. Smoke tests pass.
 
-**shadcn primitive list (18 total — locked):**
-`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `tooltip`, `popover`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`.
+**shadcn primitive list (22 total — locked):**
+`button`, `input`, `textarea`, `label`, `select`, `checkbox`, `radio-group`, `toggle-group`, `card`, `dialog`, `alert-dialog`, `alert`, `tooltip`, `hover-card`, `popover`, `sheet`, `tabs`, `skeleton`, `avatar`, `badge`, `dropdown-menu`, `combobox`.
 
-**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The `command` shadcn primitive is **deferred** — do NOT scaffold it in C1b. Document explicitly in the file list comment.
+**Note on `command`:** `cmdk` is installed (tree presence for C3/CommandMenu wiring in a later slice). The full `command` shadcn primitive is **deferred** — do NOT scaffold/export it in C1b. `combobox` uses a command-less pattern: shadcn-style `Popover` + search input + filtered listbox/options, without `Command` parts. Trade-off: enough fidelity for C1b form/search affordances, but full CommandMenu parity remains follow-up work.
 
 **Dependencies install (packages/ui is owner of shadcn primitives):**
 ```bash
@@ -96,7 +115,7 @@
 # packages/ui — shadcn primitive deps live here
 pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react \
   @radix-ui/react-dialog @radix-ui/react-popover @radix-ui/react-tooltip \
-  @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
+  @radix-ui/react-hover-card @radix-ui/react-tabs @radix-ui/react-toggle-group @radix-ui/react-select \
   @radix-ui/react-label @radix-ui/react-checkbox @radix-ui/react-radio-group \
   @radix-ui/react-avatar @radix-ui/react-slot @radix-ui/react-dropdown-menu
 ```
@@ -112,12 +131,12 @@
 ```
 
 **Files:**
-- Create: 18 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `tooltip.tsx`, `popover.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`. Each = standard shadcn CVA wrapper.
-- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). Regression test REQUIRED.
-- Modify: `packages/ui/src/index.ts` — re-export 18 shadcn primitives + cn + Button (now shadcn) + existing pickers.
+- Create: 22 shadcn primitives in `packages/ui/src/components/shadcn/`: `button.tsx` (re-export Button), `input.tsx`, `textarea.tsx`, `label.tsx`, `select.tsx`, `checkbox.tsx`, `radio-group.tsx`, `toggle-group.tsx`, `card.tsx`, `dialog.tsx`, `alert-dialog.tsx`, `alert.tsx`, `tooltip.tsx`, `hover-card.tsx`, `popover.tsx`, `sheet.tsx`, `tabs.tsx`, `skeleton.tsx`, `avatar.tsx`, `badge.tsx`, `dropdown-menu.tsx`, `combobox.tsx`. Each = standard shadcn CVA/Radix wrapper except `combobox`, which is the command-less Popover + search input + filtered list pattern.
+- Rewrite: `packages/ui/src/components/Button.tsx` — shadcn CVA Button. Variants: `default|secondary|destructive|outline|ghost|link`. Keep `primary|subtle` as aliases (primary→default, subtle→ghost). Keep `size: sm|md|lg`. **MUST preserve `loading?: boolean`**: when `loading=true`, button is `disabled`, `aria-busy="true"`, and renders a spinner via lucide-react `<Loader2 className="animate-spin" />` (16px, margin-right-2). `loading` is incompatible with `asChild`: if both are true, throw in dev (`process.env.NODE_ENV !== 'production'`); in prod, render the child without loading affordance and log a warning. Document this trade-off in a short `Button.tsx` code comment.
+- Modify: `packages/ui/src/index.ts` — re-export 22 shadcn primitives + cn + Button (now shadcn) + existing pickers.
 - Modify: `apps/frontend/src/features/admin/permissions/request-access-button.tsx:115` — `variant="primary"` → `variant="default"` (or leave with primary alias).
-- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 18 primitives once asserting no throw.
-- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression test: `loading=true` → `disabled` attribute present + `aria-busy="true"`. Must run RED before the Button rewrite (write failing test first), then GREEN.
+- Create: `packages/ui/__tests__/shadcn-smoke.test.tsx` — render each of the 22 primitives once asserting no throw.
+- Create: `packages/ui/__tests__/button-loading.test.tsx` — regression tests: `<Button loading>` → disabled + `aria-busy` + spinner; `<Button asChild><Link>…</Link></Button>` without loading → renders Link with no spinner; `<Button asChild loading>` → dev throws or logs warning, prod renders Link without spinner. Must run RED before the Button rewrite (write failing test first), then GREEN.
 
 **Dispatch (Sonnet):**
 - Read first: `packages/ui/src/components/Button.tsx` (current `loading` + `aria-busy` contract must survive), `packages/ui/src/index.ts`.
@@ -125,7 +144,7 @@
 - Constraint: failing-first for Button loading test — write test before rewrite.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck`.
 
-**Commit:** `feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix`
+**Commit:** `feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix`
 
 ---
 
@@ -168,7 +187,8 @@
 - Constraint: editor must NOT register Image extension (StarterKit image: false).
 - Constraint: no API calls. Demo route uses local state only.
 - Constraint: `@tiptap/react` lives in `packages/ui` — do NOT install it in `apps/frontend`.
-- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`.
+- Constraint: keep `apps/frontend/src/routes/dev-rich-editor.tsx` available after C2 so C3 can validate sanitizer-fail toast flow against the demo route; C5 may delete it only if no later checkpoint needs it.
+- Verification: `pnpm --filter @fops/ui test`, `pnpm --filter @fops/frontend dev`, manual visit `/dev-rich-editor`, `pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm`.
 
 **CHECKPOINT 2** — playground HTML: RichEditor surface toggle (4 surfaces), mode toggle, mention/attachment node demo, prototype rich-editor.jsx screenshot side-by-side. AskUserQuestion: "에디터 UX prototype 일치?"
 
@@ -181,12 +201,12 @@
 **Goal:** `<Toaster>` mounted in `__root.tsx`. `apiClient`, `errorMapper`, `useIdempotencyKey` land in `apps/frontend/src/lib/api/`. ALL codes in `ERROR_CODES` from `@fops/shared` covered with Korean copy and tone classification.
 
 **Files:**
-- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Fallback for unknown codes: `{ tone: 'error', message: '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.' }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
+- Create: `apps/frontend/src/lib/api/errorMapper.ts` — maps `{ code, detail }` to `{ tone: 'error'|'warning'|'info', message: string, action?: { label, run } }`. **Catalog is generated by iterating `ERROR_CODES` from `@fops/shared`** — no manually typed list. Every code must map to a non-empty Korean message. Export `GENERIC_ERROR_MESSAGE = '일시적 오류가 발생했습니다. 잠시 후 다시 시도해 주세요.'` so tests compare to the fallback constant directly. Fallback for unknown codes: `{ tone: 'error', message: GENERIC_ERROR_MESSAGE }`. Tone per category: `auth.*` → `error`, `permission.*` → `error`, `rate_limited.*` → `warning`, `validation.*` → `error`, `conflict.*` → `error` (except `conflict.stale_write` → `warning` with retry action), `not_found.*` → `error`, `internal.*` → `error`, `voc.*` → `error`, `rich_content.*` → `error`, `attachment.*` → `warning`, `reporter_facing_status.*` → `warning`. Required codes at minimum (from `packages/shared/src/errors/codes.ts`): `auth.session_invalid`, `auth.session_required`, `auth.session_expired`, `auth.workspace_mismatch`, `permission.denied`, `rate_limited.actor`, `rate_limited.ip`, `validation.failed`, `validation.malformed_request`, `validation.unknown_capability`, `conflict.idempotency_key_reuse`, `conflict.capability_already_granted`, `conflict.permission_request_duplicate`, `validation.malformed_idempotency_key`, `validation.sensitive_reason_required`, `validation.immutable_field`, `conflict.duplicate_slug`, `conflict.parent_archived`, `conflict.record_archived`, `not_found.record`, `internal.unexpected`, `voc.severity_not_user_settable`, `validation.unexpected_field`, `rich_content.disallowed_node`, `rich_content.external_image_forbidden`, `attachment.unsupported_pending_storage_slice`, `conflict.stale_write`, `voc.reporter_status_via_public_update_only`, `permission.scope_required`, `reporter_facing_status.invalid_transition`, `reporter_facing_status.gate_blocked`, `conflict.triage_already_committed`.
 - Create: `apps/frontend/src/lib/api/client.ts` — `apiClient(method, path, { body?, idempotencyKey?, ifMatch?, signal? })`. Auto-mint Idempotency-Key UUIDv4 on POST/PATCH/DELETE when not supplied. Attach `Authorization` from session store. Parse JSON. Non-2xx → throw `ApiError extends Error { code, detail, status, requestId }`. 304 → return `{ status: 304, etag }`.
 - Create: `apps/frontend/src/lib/api/useIdempotencyKey.ts` — `useIdempotencyKey(ifMatchEtag?: string)`. Returns stable UUIDv4 per render-tree until consumed; `markConsumed()` mints fresh. **When `ifMatchEtag` changes value, the hook MUST automatically mint a fresh UUIDv4** — this prevents `conflict.idempotency_key_reuse` after a stale-write refetch (BE includes `ifMatch` in the idempotency hash per `apps/backend/src/modules/voc/routes.ts:225`, so a retry with a new `If-Match` value is a semantically different request that needs a new key).
 - Create: `apps/frontend/src/lib/api/types.ts` — `ApiErrorEnvelope` matching BE `errors/envelope.ts`.
 - Modify: `apps/frontend/src/routes/__root.tsx` — mount `<Toaster position="bottom-center" />` from `sonner`.
-- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update.
+- Create: `apps/frontend/src/lib/api/__tests__/errorMapper.test.ts` — **iterate `ERROR_CODES` from `@fops/shared` and assert each maps to a non-empty Korean message** (not a manual catalog). This test auto-covers new codes added to `codes.ts` without test update. Locked assertions: (1) every `ERROR_CODES` mapping has `tone` in literal set `{ 'error', 'warning', 'info' }`; (2) every code with prefix/member in `{ voc.*, rich_content.*, attachment.*, reporter_facing_status.*, conflict.stale_write, conflict.triage_already_committed, conflict.idempotency_key_reuse }` has Korean message not equal to exported `GENERIC_ERROR_MESSAGE`; (3) `conflict.stale_write` maps to `{ tone: 'warning', action: { label: <not undefined>, run: <fn> } }`.
 - Create: `apps/frontend/src/lib/api/__tests__/client.test.ts` — mock fetch: POST mints Idempotency-Key, GET omits, Authorization attached, 422 throws ApiError, 304 returns ETag. Add test case: same `useIdempotencyKey` instance + changed `ifMatchEtag` → new key minted automatically.
 
 **Dispatch (Sonnet):**
@@ -196,7 +216,7 @@
 - Constraint: errorMapper test MUST iterate `ERROR_CODES` — no hardcoded list.
 - Verification: `pnpm --filter @fops/frontend typecheck && test`.
 
-**CHECKPOINT 2.5** — small playground demonstrating: Toaster position (bottom-center), tone variants (success / error / warning / info) with Korean copy, at least 3 representative error codes rendered as toasts (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning). AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
+**CHECKPOINT 2.5** — playground HTML demonstrating: Toaster tones + Korean copy for 3+ representative codes (`conflict.stale_write` warning + retry action, `permission.denied` error, `rate_limited.ip` warning) + RichEditor demo rendering sanitizer-rejected content with toast surfaced via apiClient mock. Requires C2 `/dev-rich-editor` route to still exist when C3 lands. AskUserQuestion: "토스트 UX + 에러 메시지 OK?"
 
 **Commit:** `feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)`
 
@@ -208,10 +228,12 @@
 
 **ADR-0020 slot contracts (port from `docs/design-prototype/components.jsx`):**
 - `PageShell` — slots: `title`, `subtitle`, `eyebrow`, `actions`, `back`, `fluid`.
-- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right).
-- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right).
+- `ListShell` — slots: `toolbar` (50px), `beforeList?`, scroll body, `detailPanel?` (right content intent only).
+- `WorkbenchShell` — slots: `toolbar` (50px), `belowToolbar?`, body, `detailPanel?` (right content intent only).
 - `ShellHeader` — shared 50px header primitive used by ListShell toolbar and WorkbenchShell toolbar.
 
+**DetailPanel ownership — LOCKED:** AppFrame owns the single global `DetailPanelSlot`. Shells DO NOT render their own detail-panel surface. `ListShell` and `WorkbenchShell` expose `detailPanel?: ReactNode`, but internally forward that content to AppFrame's slot via `useDetailPanelSlot()`. ADR-0020's "ListShell owns optional right detail panel" means the shell owns content placement intent for routes that opt into detail, not the rendering surface.
+
 **Files:**
 - Create: `packages/ui/src/layout/PageShell.tsx` — per ADR-0020 §1 + prototype `components.jsx`.
 - Create: `packages/ui/src/layout/ListShell.tsx` — per ADR-0020 §1.
@@ -223,6 +245,7 @@
 **Dispatch (Sonnet):**
 - Read first: `docs/adr/0020-*.md` (full), `docs/design-prototype/components.jsx` (source-of-truth implementations — port these, do not re-derive).
 - Constraint: shells are PURE layout — no nav, no sidebar, no rail in this chunk (those belong in AppFrame, C4b).
+- Constraint: no duplicate detail-panel surfaces. `detailPanel` props forward via `useDetailPanelSlot()` only.
 - Constraint: NO 4th shell may be created. Adding a 4th shell requires an ADR amendment per ADR-0020 Consequences.
 - Verification: `pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test`.
 
@@ -232,18 +255,25 @@
 
 ## C4b — AppFrame in apps/frontend + useFullscreenPanel hook
 
-**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
+**Goal:** `<AppFrame>` in `apps/frontend/src/lib/layout/` composes Rail(52) + Sidebar(240/56) + (shell outlet) + the single global DetailPanelSlot(440). AppFrame consumes one of the 3 shells as its inner outlet. It is NOT exported as a shell — it is the authenticated route frame. `useFullscreenPanel` hook ready for #20 to consume.
 
 **AppFrame is NOT a 4th shell.** It wraps a shell. The shell taxonomy (PageShell/ListShell/WorkbenchShell) lives in `packages/ui`. AppFrame is app-internal infrastructure.
 
 **Files:**
 - Create: `apps/frontend/src/lib/layout/AppFrame.tsx` — flex row Rail(52) + Sidebar(240/56) + Main(flex-1, shell outlet) + DetailPanelSlot(440 conditional). Headers conform to 50px rhythm via ShellHeader from `packages/ui`.
 - Create: `apps/frontend/src/lib/layout/AppRail.tsx` — 52px vertical, workspace switcher placeholder + lucide-react utility icons.
-- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse button writes `localStorage.appSidebarCollapsed`.
-- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`.
-- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook.
+- Create: `apps/frontend/src/lib/layout/AppSidebar.tsx` — 240px nav list. Slice 3 entries: Inbox, My VOCs, Triage, + New VOC. Collapse state uses SSR-safe initializer:
+  ```ts
+  const [collapsed, setCollapsed] = useState(() => {
+    if (typeof window === 'undefined') return false;
+    return localStorage.getItem('appSidebarCollapsed') === '1';
+  });
+  ```
+  Collapse button writes `localStorage.appSidebarCollapsed`.
+- Create: `apps/frontend/src/lib/layout/DetailPanelSlot.tsx` — fixed 440 right column. min 360 / max 520. Hidden when no content registered via `useDetailPanelSlot()`. This is the single rendering surface for all shell `detailPanel` content.
+- Create: `apps/frontend/src/lib/layout/useDetailPanelSlot.ts` — context-based slot register/clear hook. Forbid multiple registrants: throw in dev (and warn in prod) if called twice with overlapping lifetimes, using a ref to track active ownership.
 - Create: `apps/frontend/src/lib/panel/useFullscreenPanel.ts` — `[isFullscreen, toggle]`. Esc collapses. Route change clears.
-- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty.
+- Create: `apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx` — Rail/Sidebar/Main present; sidebar first-paint width matches stored `localStorage.appSidebarCollapsed` before any user interaction; sidebar collapse toggle writes localStorage; DetailPanelSlot collapses when empty; multiple overlapping `useDetailPanelSlot()` registrants throw/warn.
 - Create: `apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx` — toggle flips boolean; Esc dispatches collapse; route change clears (mock router).
 
 **Dispatch (Sonnet):**
@@ -251,6 +281,7 @@
 - Constraint: do not wire any route to AppFrame yet — C5 owns route mounting.
 - Constraint: AppRail content stays placeholder (entries land per-feature per AGENTS.md two-consumer rule).
 - Constraint: AppFrame is NOT exported from `packages/ui` — it lives in `apps/frontend/src/lib/layout/` only.
+- Constraint: AppFrame is the single DetailPanelSlot owner; shells forward content via hook and never mount independent right panels.
 - Verification: typecheck + test.
 
 **Commit:** `feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel`
@@ -262,18 +293,29 @@
 **Goal:** TanStack layout route `_authed.tsx` wraps all authenticated routes in `<AppFrame>`. `/vocs` file-route uses `_authed` as parent and selects the correct shell per view (ListShell for inbox/my, WorkbenchShell for triage, PageShell for create). Zod search schema validates query params. routeTree regen clean.
 
 **Route mounting — LOCKED (no "decide in dispatch" language):**
-- `apps/frontend/src/routes/_authed.tsx` — new TanStack layout route. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
-- Admin routes (`/admin/*`) are re-parented under `_authed` and classified as `PageShell`. (If full relocation is not done in C5, file a follow-up issue immediately — do not leave AppFrame-vs-admin resolution open.)
+- `apps/frontend/src/routes/_authed.tsx` — new TanStack pathless layout route. Includes a `beforeLoad` callback that calls `fetchMe()` (same auth pattern currently used in admin routes) and `redirect({ to: '/login' })` on `UnauthenticatedError`. Wraps children with `<AppFrame>`. Login + non-authed routes do NOT use this layout and do NOT render AppFrame.
+- Per-route auth guards in admin pages are DELETED in C5 after `_authed.beforeLoad` centralizes the guard.
+- Admin routes (`/admin/*`) are relocated under `_authed` and classified as `PageShell`; URL path strings remain unchanged because `_authed` is pathless:
+  - `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`
+  - `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`
+  - `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`
+  - Test file imports updated: `apps/frontend/src/routes/admin/*.test.tsx` move alongside their routes.
+  - Path strings (`/admin/managed-systems`, `/admin/analytics-areas`) UNCHANGED.
 - `__root.tsx` stays a minimal global wrapper (providers + `<Outlet>`) — no AppFrame in `__root.tsx`.
-- `/vocs` route = `apps/frontend/src/routes/_authed/vocs.tsx`. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- `/vocs` route file = `apps/frontend/src/routes/_authed/vocs.tsx`. Public path string is `/vocs`; `_authed` is pathless. Per-view shell selection: `view=inbox|my` → `<ListShell>`, `view=triage` → `<WorkbenchShell>`, `action=create` → `<PageShell>` (default when no view/action = `<ListShell>`).
+- Forbid `Route.id` usage anywhere in navigation — only `to:` path strings. Add a lint rule or grep check in C5 verification.
 
 **Files:**
-- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. Renders `<AppFrame><Outlet /></AppFrame>`.
+- Create: `apps/frontend/src/routes/_authed.tsx` — TanStack layout route. `beforeLoad` calls `fetchMe()` and redirects unauthenticated users to `/login`; renders `<AppFrame><Outlet /></AppFrame>`.
 - Create: `apps/frontend/src/routes/_authed/vocs.tsx` — `createFileRoute('/_authed/vocs')` with validateSearch zod. Component selects shell based on `view`/`action`. Main = placeholder text.
 - Modify: `apps/frontend/src/routes/__root.tsx` — minimal; providers + `<Outlet>` only (no AppFrame).
-- Relocate or re-parent: `apps/frontend/src/routes/admin/` — move under `_authed` so admin pages get AppFrame. If not done in C5, create a follow-up GitHub issue before closing this chunk.
+- Move: `apps/frontend/src/routes/admin/managed-systems.tsx` → `apps/frontend/src/routes/_authed/admin/managed-systems.tsx`.
+- Move: `apps/frontend/src/routes/admin/analytics-areas.tsx` → `apps/frontend/src/routes/_authed/admin/analytics-areas.tsx`.
+- Move: `apps/frontend/src/routes/admin/placeholder.tsx` → `apps/frontend/src/routes/_authed/admin/placeholder.tsx`.
+- Move: `apps/frontend/src/routes/admin/*.test.tsx` → alongside relocated `_authed/admin/*` routes; update imports.
+- Modify relocated admin routes — delete page-local auth guards now covered by `_authed.beforeLoad`.
 - Auto-regen: `apps/frontend/src/routeTree.gen.ts` (TanStackRouterVite plugin handles on dev).
-- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/_authed/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/_authed/vocs?action=create`; `view=triage` renders WorkbenchShell.
+- Create: `apps/frontend/src/routes/__tests__/vocs.test.tsx` — navigating to `/vocs?view=inbox` resolves; invalid `?view=foo` rejected by zod; sidebar `+ New VOC` href = `/vocs?action=create`; `view=triage` renders WorkbenchShell.
 - Delete: `apps/frontend/src/routes/dev-rich-editor.tsx` IF it was added in C2 and is no longer needed (or keep behind DEV flag).
 
 **Search schema:**
@@ -292,9 +334,10 @@
 - Read first: `apps/frontend/src/routes/__root.tsx`, `apps/frontend/vite.config.ts`, `apps/frontend/src/routes/admin/managed-systems.tsx` (existing TanStack file-route pattern), `apps/frontend/src/lib/layout/AppFrame.tsx` (just created in C4b), `packages/ui/src/layout/` (shells).
 - Constraint: zero feature logic in /vocs — placeholder only.
 - Constraint: login + non-authed routes must NOT render AppFrame — test this.
-- Verification: `pnpm --filter @fops/frontend dev`, navigate `/_authed/vocs?view=inbox`, `/_authed/vocs?action=create`. `pnpm typecheck && test`.
+- Constraint: no `Route.id` navigation. Run `grep -rn 'Route\\.id\\|from: .*Route\\.id\\|to: .*Route\\.id' apps/frontend/src/` and refactor any hits to literal `to:` path strings.
+- Verification: `pnpm --filter @fops/frontend dev`, navigate `/vocs?view=inbox`, `/vocs?action=create`. `pnpm typecheck && test`.
 
-**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, prototype `screen-voc.jsx` side-by-side, /vocs route screenshot per shell variant. AskUserQuestion: "셸 정렬 OK?"
+**CHECKPOINT 3 (C4b+C5 merged)** — playground HTML: AppFrame layout with collapse toggle, fullscreen toggle, 50px header overlay, baseline screenshot comparison per `docs/design-prototype/screenshots/final-baselines/manifest.json` route field, `/vocs` route screenshot per shell variant, and `mustSurvive` text validated for each matched route. AskUserQuestion: "셸 정렬 OK?"
 
 **Commit:** `feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view`
 
@@ -306,7 +349,7 @@
 
 **Tasks:**
 1. `pnpm typecheck && pnpm lint && pnpm test && pnpm build` — all clean from monorepo root.
-2. Playwright MCP: screenshot `/_authed/vocs?view=inbox` + `/_authed/vocs?action=create` + prototype `screen-voc.jsx` + `screen-voc-create.jsx`. Embed in final report.
+2. Playwright MCP: screenshot `/vocs?view=inbox` + `/vocs?action=create` and compare against `docs/design-prototype/screenshots/final-baselines/*.png` matched per `manifest.json` `route` field. Validate the `mustSurvive` text for each route. Embed in final report.
 3. Cycle 1: `codex review --base develop - <<'EOF' …` brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-1-codex.md`.
 4. Reinforce gaps via Sonnet dispatch (narrow file set per finding).
 5. Cycle 2: dispatch Opus subagent (general-purpose model=opus) with adversarial brief. Output → `.review/SLICE-3-18-REVIEW-CYCLE-2.md`.
@@ -326,8 +369,11 @@
 - No chunk touches files outside its declared file set without re-dispatch.
 - Sonnet dispatches read AGENTS.md hierarchy first (`apps/frontend/AGENTS.md` mandatory).
 - Tests added per chunk run live, not deferred.
+- Vitest must run in `@fops/ui` before any test in C1a/C1b/C2/C4a is meaningful (P1-A fix in C1a step 0).
 - **Token snapshot test is the gate — no chunk merges if token fidelity test fails.**
+- **Token-fidelity test covers ALL Pack 17 token classes: colors + layout + spacing + radius + shadow + typography + status/severity/confidence, not just colors.**
 - **AppFrame in `apps/frontend` NEVER becomes a 4th exported shell. Shell taxonomy = 3 (PageShell/ListShell/WorkbenchShell) per ADR-0020.**
+- **AppFrame is the single DetailPanelSlot owner; shells forward `detailPanel` content via `useDetailPanelSlot()` (P2-D).**
 - `git add -A` BANNED — explicit file paths only (per memory `feedback_orchestration`).
 - Failing-first (RED → GREEN) discipline required for: token-fidelity snapshot (C1a), Button loading regression (C1b).
 
@@ -341,6 +387,7 @@
 - TipTap `generateHTML` from `@tiptap/html` requires same extension set on render side → ensure renderer imports custom extensions, not just StarterKit.
 - TanStack Router file routes regen — ensure vite plugin runs during typecheck CI (currently dev-only?).
 - **shadcn HSL CSS vars must be remapped to R G B triple references** — `semantic.css` must explicitly remap `--background`, `--foreground`, `--primary`, etc. to `rgb(var(--color-X))` form; do NOT inherit shadcn defaults which use HSL.
-- **TanStack layout route `_authed.tsx` adoption may require relocating `admin/*` routes — if not done in C5, file a follow-up issue immediately** before closing C5.
+- **TanStack `_authed` layout adoption requires admin relocation + auth-guard centralization. If `Route.id` is referenced anywhere, refactor in C5 (grep first).**
 - `@tiptap/react` in `packages/ui` — verify `apps/frontend` resolves it transitively via workspace link without explicit install; if pnpm strict-mode blocks it, add a peer dep declaration.
+- lucide-react peer-dep React 19: current frontend has 0.469.0; npm has 0.470.0+ available. Bump to `>=0.470` if peer range supports React 19, or set `pnpm install --strict-peer-dependencies=false` in CI.
 - Tailwind v3 vs v4 ambiguity — DESIGN.md Quick Start shows `@theme` (v4 syntax). Project uses Tailwind 3. Subagent must NOT use v4 `@theme` blocks in any CSS files.
diff --git a/report/2026-05-20-slice3-18-plan-playground.html b/report/2026-05-20-slice3-18-plan-playground.html
index df623755bc71b7eb654265f94cb547bcbd54e0c7..aa0c8d3fb29dae5b489afb8c132524c077b62ac4
--- a/report/2026-05-20-slice3-18-plan-playground.html
+++ b/report/2026-05-20-slice3-18-plan-playground.html
@@ -119,7 +119,7 @@
     <div class="sect">
       <h3>Summary</h3>
       <div style="font-size:11.5px;color:var(--mute);line-height:1.6">
-        Locks: Pack 17 light (R G B triples) · shadcn @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy.<br><br>
+        Locks: Pack 17 light (R G B triples) · 22 shadcn primitives @ <code>packages/ui/src/components/shadcn/</code> · Button replace + loading preserved · 10 chunks · ADR-0020 3-shell taxonomy amended by ADR-0021 in C0.<br><br>
         Out of scope → #19 Create / #20 Inbox+Detail / #21 Triage+composers.
       </div>
     </div>
@@ -142,52 +142,58 @@
   {
     id:'c0', name:'C0 — ADR-0021 + supersede', dot:'var(--c0)', model:'haiku', checkpoint:null,
     risk:'low', loc:'~80',
-    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016. Documents R G B vs hex two-format rule. No code changes.',
+    goal:'ADR-0021 (Pack 17 Light) lands superseding ADR-0016 and amending ADR-0020 out-of-scope light-theme clause. Documents R G B vs hex two-format rule. No code changes.',
     files:[
       {type:'create', path:'docs/adr/0021-pack-17-samsung-light-design-system.md  // R G B triple runtime, hex in DESIGN.md'},
-      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'}
+      {type:'modify', path:'docs/adr/0016-*.md  // add superseded-by: 0021'},
+      {type:'modify', path:'docs/adr/0020-shell-taxonomy-three-route-shells-and-50px-header-rhythm.md  // remove light-theme out-of-scope; cross-link ADR-0021'}
     ],
     install:null,
-    dispatch:'Haiku subagent. Doc-only. 1 create + 1 edit. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex.',
+    dispatch:'Haiku subagent. Doc-only. 1 create + 2 edits. ADR-0021 must document two-format split: runtime tokens.css = R G B triple (Tailwind <alpha-value>); DESIGN.md + fixture = hex. ADR-0020 light-theme out-of-scope clause is removed and cross-linked.',
     verify:'git diff review only',
-    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016'
+    commit:'docs(slice3 #18): ADR-0021 Pack 17 Samsung-light supersedes ADR-0016, amends ADR-0020 Out-of-scope'
   },
   {
     id:'c1a', name:'C1a — Tokens + Tailwind preset + snapshot', dot:'var(--c1)', model:'sonnet', checkpoint:'CHECKPOINT 1',
     risk:'high', loc:'~300',
-    goal:'Pack 17 tokens in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity snapshot frozen against styles.css. Failing-first discipline.',
+    goal:'Step 0 wires Vitest in @fops/ui, then Pack 17 tokens land in R G B triple format. Tailwind preset with <alpha-value>. Token-fidelity covers full Pack 17 surface, and app route token classnames are audited/renamed.',
     files:[
+      {type:'create', path:'packages/ui/vitest.config.ts  // jsdom + globals'},
+      {type:'create', path:'packages/ui/vitest.setup.ts  // @testing-library/jest-dom'},
+      {type:'modify', path:'packages/ui/package.json  // test=vitest run; test:watch=vitest'},
       {type:'rewrite', path:'packages/ui/src/styles/tokens.css  // R G B triple, not hex'},
       {type:'rewrite', path:'packages/ui/src/styles/semantic.css  // remap shadcn HSL vars to R G B refs'},
       {type:'rewrite', path:'packages/ui/tailwind.preset.ts  // rgb(var(--X) / <alpha-value>), Tailwind 3 syntax'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // {tokenName, hex, rgb} for 19 --color-* tokens'},
-      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // RED first, then GREEN after port'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.fixture.ts  // all Pack 17 tokens: colors/layout/spacing/radius/shadow/typography/status/severity/confidence'},
+      {type:'create', path:'packages/ui/src/styles/__tests__/token-fidelity.test.ts  // postcss parse; set equality + value match'},
+      {type:'modify', path:'apps/frontend/src/routes/{admin/managed-systems,admin/analytics-areas,login,index}.tsx  // token classname rename audit'},
+      {type:'create', path:'apps/frontend/src/__tests__/token-class-coverage.test.ts  // resolveConfig coverage for TSX token classes'},
       {type:'modify', path:'apps/frontend/src/styles.css  // @import tokens.css + semantic.css'}
     ],
-    install:null,
-    dispatch:'Sonnet. Read docs/design-prototype/styles.css + current tokens/semantic/preset. Failing-first: write token-fidelity.test.ts RED before porting. Convert hex → R G B: #RRGGBB → R G B decimal. Examples: #f3f7fe→243 247 254, #1428a0→20 40 160.',
-    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
+    install:'pnpm --filter @fops/ui add -D vitest jsdom @testing-library/react @testing-library/jest-dom @vitejs/plugin-react @types/node postcss',
+    dispatch:'Sonnet. Step 0: wire Vitest and prove RED with placeholder failing test before any other test. Read docs/design-prototype/styles.css lines 1-200 + current tokens/semantic/preset. Grep route token classes, map text-status-danger→text-accent-danger/text-danger and border-surface-overlay→border-default. Token test uses postcss, not regex.',
+    verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test; run frontend token-class coverage test and confirm no unresolved Tailwind theme keys',
     commit:'feat(slice3 #18): Pack 17 tokens (R G B) + Tailwind preset + token-fidelity snapshot'
   },
   {
     id:'c1b', name:'C1b — shadcn baseline + Button loading', dot:'var(--c1)', model:'sonnet', checkpoint:null,
     risk:'high', loc:'~600',
-    goal:'18 shadcn primitives (adds dropdown-menu, defers command). Button CVA with loading preserved + aria-busy + spinner. Regression test RED→GREEN.',
+    goal:'22 shadcn primitives (adds alert, hover-card, sheet, combobox; command deferred). Button CVA with loading preserved; loading is incompatible with asChild.',
     files:[
-      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,tooltip,popover,tabs,skeleton,avatar,badge,dropdown-menu}.tsx  // 18 total; command deferred'},
-      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2 spinner'},
-      {type:'modify', path:'packages/ui/src/index.ts  // re-export 18 primitives + Button'},
+      {type:'create', path:'packages/ui/src/components/shadcn/{button,input,textarea,label,select,checkbox,radio-group,toggle-group,card,dialog,alert-dialog,alert,tooltip,hover-card,popover,sheet,tabs,skeleton,avatar,badge,dropdown-menu,combobox}.tsx  // 22 total; command deferred; combobox command-less'},
+      {type:'rewrite', path:'packages/ui/src/components/Button.tsx  // CVA + loading + aria-busy + Loader2; asChild+loading dev throw, prod warning/no spinner'},
+      {type:'modify', path:'packages/ui/src/index.ts  // re-export 22 primitives + Button'},
       {type:'modify', path:'apps/frontend/src/features/admin/permissions/request-access-button.tsx:115  // variant primary→default'},
-      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 18 primitives'},
-      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // RED before rewrite, GREEN after'}
+      {type:'create', path:'packages/ui/__tests__/shadcn-smoke.test.tsx  // render each of 22 primitives'},
+      {type:'create', path:'packages/ui/__tests__/button-loading.test.tsx  // loading, asChild, asChild+loading dev/prod contract'}
     ],
     install:`# packages/ui — shadcn primitive deps + TipTap (pre-install for C2)
-pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
+pnpm --filter @fops/ui add class-variance-authority tailwind-merge clsx lucide-react @radix-ui/react-{dialog,popover,hover-card,tooltip,tabs,toggle-group,select,label,checkbox,radio-group,avatar,slot,dropdown-menu} @tiptap/core @tiptap/pm @tiptap/html @tiptap/react @tiptap/starter-kit @tiptap/extension-link @tiptap/extension-underline @tiptap/extension-placeholder
 # apps/frontend — app-level only; NO direct @tiptap/* (consumed via @fops/ui)
 pnpm --filter @fops/frontend add sonner cmdk @tailwindcss/typography`,
-    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred — document explicitly.',
+    dispatch:'Sonnet. Read packages/ui/src/components/Button.tsx (loading + aria-busy must survive). Failing-first: write button-loading.test.tsx RED before rewrite. command primitive deferred; combobox uses Popover + search input + filtered list without Command parts. Add Button code comment for asChild/loading trade-off.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test && pnpm --filter @fops/frontend typecheck',
-    commit:'feat(slice3 #18): 18 shadcn primitives + Button CVA with loading regression fix'
+    commit:'feat(slice3 #18): 22 shadcn primitives + Button CVA with loading regression fix'
   },
   {
     id:'c1c', name:'C1c — Picker rebuild + caller migration', dot:'var(--c1)', model:'sonnet', checkpoint:null,
@@ -216,74 +222,75 @@
     ],
     install:'(none — TipTap pre-installed in C1b in packages/ui)',
     dispatch:'Sonnet. Read rich-editor.jsx + ADR-0011 + BE sanitizer.ts. Constraints: surface = opaque pass-through; StarterKit image:false; demo = local state only; @tiptap/* must NOT be installed in apps/frontend.',
-    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor',
+    verify:'pnpm --filter @fops/ui test && manual visit /dev-rich-editor && pnpm dedupe --check react react-dom @tiptap/react @tiptap/pm',
     commit:'feat(slice3 #18): TipTap RichEditor + RichContentRenderer + attachmentRef + mention extensions'
   },
   {
     id:'c3', name:'C3 — sonner + apiClient + errorMapper', dot:'var(--c3)', model:'sonnet', checkpoint:'CHECKPOINT 2.5',
     risk:'med', loc:'~500',
-    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES — full coverage, Korean copy.',
+    goal:'<Toaster> mounted. apiClient (If-Match, Idempotency-Key auto). useIdempotencyKey(ifMatchEtag) auto-mints on etag change. errorMapper iterates ERROR_CODES with rigorous tone/fallback/action assertions.',
     files:[
-      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts'},
+      {type:'create', path:'apps/frontend/src/lib/api/{client,errorMapper,useIdempotencyKey,types}.ts  // export GENERIC_ERROR_MESSAGE'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // <Toaster position="bottom-center"/>'},
       {type:'create', path:'apps/frontend/src/lib/api/__tests__/{errorMapper,client}.test.ts'}
     ],
     install:'(none — sonner installed in C1b)',
-    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260 (If-Match in idempotency hash). useIdempotencyKey(ifMatchEtag?) auto-mints fresh UUID when etag changes.',
+    dispatch:'Sonnet. Read packages/shared/src/errors/codes.ts (iterate ERROR_CODES, do NOT hardcode list) + envelope.ts + voc routes.ts lines 200-260. Tests assert tone literal set, non-generic messages for VOC/rich/attachment/reporter/conflict special codes, and conflict.stale_write warning action. Checkpoint includes /dev-rich-editor sanitizer-reject toast via apiClient mock.',
     verify:'pnpm --filter @fops/frontend typecheck && test',
     commit:'feat(slice3 #18): sonner Toaster + apiClient + errorMapper (full ERROR_CODES) + useIdempotencyKey(ifMatch)'
   },
   {
     id:'c4a', name:'C4a — Three shells in packages/ui', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~400',
-    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Re-exported from index.ts. No 4th shell.',
+    goal:'PageShell + ListShell + WorkbenchShell + ShellHeader in packages/ui/src/layout/. Per ADR-0020. Shell detailPanel props forward to AppFrame slot; no shell-owned panel surface.',
     files:[
       {type:'create', path:'packages/ui/src/layout/PageShell.tsx  // slots: title,subtitle,eyebrow,actions,back,fluid'},
-      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // slots: toolbar(50px),beforeList?,body,detailPanel?'},
-      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // slots: toolbar(50px),belowToolbar?,body,detailPanel?'},
+      {type:'create', path:'packages/ui/src/layout/ListShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
+      {type:'create', path:'packages/ui/src/layout/WorkbenchShell.tsx  // detailPanel? forwarded via useDetailPanelSlot'},
       {type:'create', path:'packages/ui/src/layout/ShellHeader.tsx  // 50px via var(--toolbar-height), NOT hardcoded px'},
       {type:'modify', path:'packages/ui/src/index.ts  // re-export 3 shells + ShellHeader'},
       {type:'create', path:'packages/ui/__tests__/shell-taxonomy.test.tsx  // 3 shells render; ShellHeader=50px; slot smoke'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. NO 4th shell.',
+    dispatch:'Sonnet. Read docs/adr/0020-*.md (full) + docs/design-prototype/components.jsx (port, do not re-derive). Constraint: PURE layout shells — no nav/rail/sidebar here. AppFrame owns DetailPanelSlot; shells only forward content intent via hook. NO 4th shell.',
     verify:'pnpm --filter @fops/ui typecheck && pnpm --filter @fops/ui test',
     commit:'feat(slice3 #18): PageShell + ListShell + WorkbenchShell + ShellHeader per ADR-0020'
   },
   {
     id:'c4b', name:'C4b — AppFrame + useFullscreenPanel', dot:'var(--c4)', model:'sonnet', checkpoint:null,
     risk:'med', loc:'~500',
-    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+DetailPanelSlot. NOT a 4th shell — app-internal only. useFullscreenPanel hook.',
+    goal:'AppFrame in apps/frontend/src/lib/layout/ composes Rail+Sidebar+shell outlet+the single global DetailPanelSlot. SSR-safe sidebar collapse. NOT a 4th shell.',
     files:[
       {type:'create', path:'apps/frontend/src/lib/layout/AppFrame.tsx  // NOT exported as shell; wraps one of 3 shells'},
       {type:'create', path:'apps/frontend/src/lib/layout/AppRail.tsx'},
-      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // collapse→localStorage'},
-      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // 440px, min 360, max 520'},
-      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts'},
+      {type:'create', path:'apps/frontend/src/lib/layout/AppSidebar.tsx  // SSR-safe localStorage initializer; first-paint width test'},
+      {type:'create', path:'apps/frontend/src/lib/layout/DetailPanelSlot.tsx  // single global 440px slot, min 360, max 520'},
+      {type:'create', path:'apps/frontend/src/lib/layout/useDetailPanelSlot.ts  // forbid overlapping registrants'},
       {type:'create', path:'apps/frontend/src/lib/panel/useFullscreenPanel.ts  // Esc collapses, route change clears'},
       {type:'create', path:'apps/frontend/src/lib/layout/__tests__/AppFrame.test.tsx'},
       {type:'create', path:'apps/frontend/src/lib/panel/__tests__/useFullscreenPanel.test.tsx'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet — it is NOT a shell itself, NOT in packages/ui.',
+    dispatch:'Sonnet. Read docs/design-prototype/app.jsx + ADR-0020 + spec voc.md §6.7 + packages/ui/src/layout/ (shells from C4a). AppFrame consumes shells as outlet, owns the only DetailPanelSlot, and is NOT in packages/ui.',
     verify:'typecheck + test',
     commit:'feat(slice3 #18): AppFrame (Rail+Sidebar+Main+DetailPanelSlot) + useFullscreenPanel'
   },
   {
     id:'c5', name:'C5 — _authed layout route + /vocs shell', dot:'var(--c5)', model:'sonnet', checkpoint:'CHECKPOINT 3 (C4b+C5 merged)',
     risk:'med', loc:'~300',
-    goal:'_authed.tsx layout route wraps authed routes in AppFrame. /vocs selects shell per view (ListShell/WorkbenchShell/PageShell). Admin routes re-parented under _authed. __root.tsx minimal.',
+    goal:'_authed.tsx pathless layout centralizes auth beforeLoad(fetchMe) + AppFrame. /vocs selects shell per view. Admin routes move under _authed with URL paths unchanged. __root.tsx minimal.',
     files:[
-      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // layout route: <AppFrame><Outlet/></AppFrame>'},
-      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // shell selection per view/action'},
+      {type:'create', path:'apps/frontend/src/routes/_authed.tsx  // beforeLoad fetchMe; redirect /login on UnauthenticatedError; <AppFrame><Outlet/></AppFrame>'},
+      {type:'create', path:'apps/frontend/src/routes/_authed/vocs.tsx  // public path /vocs; shell selection per view/action'},
       {type:'modify', path:'apps/frontend/src/routes/__root.tsx  // minimal: providers + <Outlet> only'},
-      {type:'modify', path:'apps/frontend/src/routes/admin/  // re-parent under _authed as PageShell (or file follow-up)'},
-      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // shell per view + login NOT in AppFrame'},
+      {type:'move', path:'apps/frontend/src/routes/admin/{managed-systems,analytics-areas,placeholder}.tsx → apps/frontend/src/routes/_authed/admin/  // paths /admin/* unchanged; delete per-page auth guards'},
+      {type:'move', path:'apps/frontend/src/routes/admin/*.test.tsx → apps/frontend/src/routes/_authed/admin/  // update imports'},
+      {type:'create', path:'apps/frontend/src/routes/__tests__/vocs.test.tsx  // /vocs shell per view + login NOT in AppFrame + no Route.id nav'},
       {type:'delete', path:'apps/frontend/src/routes/dev-rich-editor.tsx  // if not kept behind DEV flag'}
     ],
     install:'(none)',
-    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame.',
-    verify:'dev server → /_authed/vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). pnpm typecheck && test',
+    dispatch:'Sonnet. Read __root.tsx + vite.config.ts + admin/managed-systems.tsx + AppFrame.tsx (C4b) + shells (C4a). LOCKED: _authed.tsx owns auth + AppFrame mount; __root.tsx stays minimal; login/non-authed skip AppFrame; use only to: path strings, never Route.id.',
+    verify:'dev server → /vocs?view=inbox (ListShell) + ?view=triage (WorkbenchShell) + ?action=create (PageShell). grep for Route.id nav. pnpm typecheck && test',
     commit:'feat(slice3 #18): _authed layout route + /vocs route shell + shell selection per view'
   },
   {
@@ -298,7 +305,7 @@
     ],
     install:'(none)',
     dispatch:'Cycle 1 = codex CLI (external). Cycle 2 = Opus subagent. Each cycle followed by Sonnet reinforcement.',
-    verify:'pnpm typecheck && lint && test && build all green. Playwright /_authed/vocs?view=inbox + ?view=triage + ?action=create vs prototype screenshots.',
+    verify:'pnpm typecheck && lint && test && build all green. Playwright /vocs?view=inbox + ?view=triage + ?action=create vs final-baselines/*.png by manifest route and mustSurvive text.',
     commit:'(squash-merge on develop)'
   }
 ];

hook: Stop
hook: Stop Completed
tokens used
114,433
Updated the two requested files:

- [.review/SLICE-3-18-PLAN.md](/Users/hyojung/Desktop/2026/FeedbackOps/.review/SLICE-3-18-PLAN.md)
- [report/2026-05-20-slice3-18-plan-playground.html](/Users/hyojung/Desktop/2026/FeedbackOps/report/2026-05-20-slice3-18-plan-playground.html)

Covered all cycle-2 findings: 22 shadcn primitives, C0 ADR-0020 amendment, C1a Vitest prerequisite and token-class audit, stricter token-fidelity/errorMapper tests, `_authed` pathless auth mechanics, Button `asChild + loading` contract, SSR-safe sidebar collapse, single AppFrame DetailPanelSlot ownership, C3 sanitizer toast flow, TipTap dedupe check, and manifest-based Playwright baseline validation.

I also updated the playground CHUNKS array and summary blurb. Verification scan passed for the requested markers, and the playground CHUNKS array still parses as 10 chunks. I did not edit source code, ADRs, package files, or install dependencies. Note: `DESIGN.md` was already dirty in the worktree; I did not touch it.

Source used for the lucide risk note: npm package listing for `lucide-react` showing `0.470.0+` availability: https://www.npmjs.com/package/lucide-react

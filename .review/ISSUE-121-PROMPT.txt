ROLE: You are CODEX, implementing issue #121 in a sandboxed git worktree. You write code; a separate VERIFIER proves it green outside the sandbox (you have NO network/DB access — do not try to run migrations or DB tests; they will EPERM). Write code + tests; rely on the VERIFIER for the DB-backed green signal.

ISSUE #121 — Slice 5: Entity-link provider registry + Finding foundation.
This is the FOUNDATION for #122-#126. The SIGNED CONTRACT is docs/adr/0024-finding-as-entity-link-target-and-provider-registry.md (read it first, fully) plus docs/implementation/06-entity-linking-contract.md (Slice 5 registry entry), docs/design/15-data-contracts.md (Finding table), docs/implementation/05-permission-policy.md (capabilities).

NON-NEGOTIABLE CONSTRAINTS:
- PRESERVE #112-#115 VOC↔VOC `related_to` behavior byte-for-byte. The existing suite apps/backend/src/modules/entity-links/__tests__/entity-links.integration.test.ts must still pass unchanged in intent — EXTEND it, do not rewrite it. The tests at the "unsupported tuple" cases (~lines 250-263) currently assert that `finding`/`evidence_of` inputs FAIL; update ONLY those to reflect the new allowlist, and add new finding-target coverage.
- `finding` is added as a `target_type` ONLY (never `source_type`) this slice.
- entity_links widening MUST be a COMPOSITE TUPLE CHECK allowing exactly `(voc,voc,related_to)` and `(voc,finding,created_finding)`. Independent per-column value CHECKs are FORBIDDEN (they would admit invalid tuples like voc→voc evidence_of, finding→voc).
- Creatable visibility stays `internal_only` (do NOT change creatableEntityLinkVisibilitySchema's literal).
- Finding lives in its OWN Postgres schema `finding` (tables finding.findings), matching the per-module `voc` schema pattern in apps/backend/src/db/schema/voc.ts. Add `finding` to drizzle.config.ts schemaFilter.

SCOPE — files you may touch:
1. packages/shared/src/enums/capabilities.ts — add 'finding.read' and 'finding.manage' to CAPABILITIES + CAPABILITY_META (both { sensitive: false }).
2. packages/shared/src/enums/audit-events.ts — add 'finding_created_from_voc' to AUDIT_EVENT_TYPES + a findingCreatedFromVocDetailSchema (fields: finding_id uuid, source_voc_id uuid, primary_managed_system_id uuid, source_type literal 'voc'); register it in AUDIT_EVENT_DETAIL_SCHEMAS. Broaden entityLinkCreatedDetailSchema to an ADDITIVE discriminated union: keep the existing {source:voc,target:voc,relation:related_to,visibility:internal_only} member, ADD {source:voc,target:finding,relation:created_finding,visibility:internal_only}. Do the same shape extension for the link audit if needed. Export updated types.
3. packages/shared/src/entity-links.ts — add 'finding' to the entity-type enum used for link endpoints; add 'created_finding' to the relation-type enum; keep creatable visibility literal 'internal_only'. Add a registered-pairs notion if the schema validates pairs. Add minimal FindingRef if needed. DO NOT break existing exports used by #112-#115.
4. apps/backend/src/db/schema/finding.ts — NEW. `finding.findings` table per docs/design/15-data-contracts.md:122-145: id uuid pk, workspace_id fk, primary_managed_system_id fk (NOT NULL), title text NOT NULL, summary text NOT NULL, source_type text CHECK in ('voc','voc_cluster','survey','manual'), source_id uuid (CHECK: NOT NULL unless source_type='manual'), evidence_count int NOT NULL default 0 CHECK >=0, severity text CHECK in ('low','medium','high','critical') NOT NULL, confidence text CHECK in ('low','medium','high') (nullable), status text CHECK in ('draft','active','not_actionable','converted','archived') NOT NULL default 'draft', analytics_area_id uuid (nullable) fk, linked_task_id uuid nullable, linked_milestone_id uuid nullable, created_by uuid fk actors NOT NULL, created_at/updated_at timestamptz NOT NULL default now(). Mirror conventions in voc.ts (vocSchema = pgSchema('voc'); create findingSchema = pgSchema('finding')). Add a workspace+MS index for list. Export `findings`.
5. apps/backend/src/db/schema/core.ts — replace the independent source_type/target_type/relation_type CHECKs on entity_links with a COMPOSITE TUPLE CHECK (allowed tuples above). Add `finding` to target_type domain via the tuple check only. Add an entity_links_workspace_relation_idx on (workspace_id, relation_type). Preserve activeUniqueIdx, partial source/target indexes, notSelfCheck.
6. apps/backend/migrations/0020_*.sql — NEW hand-written SQL. CREATE SCHEMA finding; GRANTs for fops_app (SELECT/INSERT/UPDATE as the voc tables get — match the grant pattern in 0018_entity_links.sql / voc migrations; fops_app must NOT get DELETE) and fops_migrate; CREATE TABLE finding.findings (full DDL incl. all CHECKs, FKs, index); DROP the old named entity_links CHECK constraints and ADD the composite tuple CHECK + the workspace_relation index. IMPORTANT (known gotcha): `drizzle-kit generate` does NOT emit GRANTs/CHECKs/partial-indexes and hits a pre-existing 0003/0004 snapshot collision — so HAND-WRITE the SQL and HAND-REGISTER the migration in apps/backend/migrations/meta/_journal.json + create the 0020 snapshot entry following exactly how 0018/0019 were registered. Study 0018_entity_links.sql + 0019_entity_link_detach.sql + the meta journal for the exact pattern.
7. apps/backend/src/modules/entity-links/ — introduce a PROVIDER REGISTRY. Define the provider interface (entity_type, assertExists(id, workspace_id), getPermissionSubject(id) → managed_system_id/reporter context for authz, getReporterSummary(id), getInternalSummary(id), listExpectedLinks(id)) per 06:47-58. Refactor the service so endpoint resolution + read-authz dispatch through the registry instead of the hard-coded resolveVocEndpoint/voc.read branches. Register the `voc` provider (wrapping the existing VOC resolution + voc.read authz, behavior-preserving). Register the `finding` provider: assertExists via finding.findings; getPermissionSubject → the finding's primary_managed_system_id; read-authz = finding.read on that MS; getReporterSummary returns UNAVAILABLE (so targetSummaryAvailable=false); getInternalSummary → minimal finding read model. Add the `(voc → finding, created_finding)` pair to the creatable allowlist. Do NOT implement POST /vocs/:id/create-finding here (that is #122) — only make the registry + finding provider exist and be unit/integration-testable via seeded rows.
8. apps/backend/src/modules/findings/ — minimal repo (repo.ts / repo-read.ts) needed by the finding provider (assertExists + read model). Follow the voc module's repo conventions. No routes yet.
9. apps/backend/src/modules/entity-links/__tests__/entity-links.integration.test.ts — EXTEND: add finding-target rows to the visibility matrix asserting ADR-0024 §D cells (Reporter hidden, User hidden, Dev in-scope allowed [both endpoints readable], Dev out-scope hidden, Admin allowed; mixed-readability → hidden). Add a test that seeding a (voc,finding,created_finding) row is allowed by the tuple CHECK and an invalid tuple (e.g. voc,finding,related_to or finding,voc,...) is rejected. Update the prior "unsupported tuple" assertions to the new allowlist. You may add a seed helper for findings.

ABORT-WITH-BLOCKER if your touch set must escape the files above (write .review/ISSUE-121-BLOCKER.json with a real reason_code + blocking_fact — never the example text). If a contract in ADR-0024 is ambiguous or contradicts the code, stop and write the blocker rather than guessing.

When done, write .review/ISSUE-121-CODEX.json: { "issue": 121, "status": "done", "files_touched": [...], "migration": "0020_...", "tests_added": [...], "notes": "...", "preserved_112_115": true }.

Style: match surrounding code (Drizzle, Fastify, zod, snake_case DB / camelCase TS). Keep the diff minimal and reviewable.
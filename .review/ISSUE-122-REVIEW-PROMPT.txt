You are an adversarial read-only REVIEWER for issue #122 in this worktree. Inspect the working-tree changes with `git --no-pager diff develop -- apps packages` and judge against docs/adr/0024-finding-as-entity-link-target-and-provider-registry.md, docs/implementation/03-api-contracts.md:331-433, docs/design/15-data-contracts.md:122-155, docs/design/05-finding-insight-system.md.

Check HARD (file:line):
1. ATOMICITY: does createFindingFromVoc write finding + created_finding link + audit finding_created_from_voc + audit entity_link.created in ONE db transaction? Any path that opens a second/nested transaction or that can leave a finding without its link (or vice-versa)? Does it reuse the #121 tx-taking insertActiveEntityLink rather than a self-transacting createLink?
2. SOURCE-SHAPED ROUTE (03:337-342): does POST /vocs/:id/create-finding only PARSE in the voc route and delegate the writes to the findings service? Any direct write to finding.findings or core.entity_links from the voc route = violation.
3. AUTHZ: create requires finding.manage on the target MS AND voc.read on the SOURCE VOC's MS. Unreadable source VOC → 404 (not 403, no existence leak). Reporter/User denied. Verify the source-read check actually runs BEFORE any write. Is finding.read enforced on GET /findings and GET /findings/:id (permission-blocked distinct from 404)?
4. PROVENANCE & AA RULE: source_type='voc'/source_id set from the route VOC and not mutable; evidence_count starts 0; analytics_area_id (if provided) validated to belong to primary_managed_system_id (422 otherwise) — matches the VOC create-service pattern?
5. CONTRACT SHAPE: endpoints + DTO match 03 + 15 (severity/confidence/status enums, summary is plain text). Idempotency frame matches POST /vocs convention + error-code vocabulary.
6. SCOPE/ROLE-BLEED: anything touched outside findings module + voc routes + server + packages/shared/findings? Any entity_links schema/migration change (should be none)?
7. CORRECTNESS: any bug failing VERIFY or breaking #123/#124.

Rank BLOCKER/MAJOR/MINOR. Write verdict to .review/ISSUE-122-REVIEW.json {"issue":122,"verdict":"approve|approve_with_nits|request_changes","blockers":[],"major":[],"minor":[],"summary":""} if writable; also print the summary.

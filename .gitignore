node_modules
dist
build
.turbo
coverage
.env
.env.local
.env.*.local
*.log
.DS_Store
.vite
# TanStack Router generated route tree
apps/frontend/src/routeTree.gen.ts
playwright-report
test-results

# Local Claude Code harness settings — per-developer
.claude/settings.json
report/

# MinIO local data volume (docker-compose.dev.yml)
minio_data/

# CodeGraph local index (per-developer)
.codegraph/

# Per-developer agent/tool caches and ephemeral artifacts
.playwright-mcp/
docs/.llmwiki/
.claude/worktrees/
smoke-*.png
visual-*.png
# Workflow toolkit lives in a separate repo (see project-workflow-extraction);
# never commit a symlink into it — product repo stays product-only.
scripts/codex-safe.sh
# install-into.sh --mode symlink output: absolute machine-local links
.agent-workflow/
.claude/skills/agent-workflow
# 2026-08-29 generic toolkit install: added runtime-mirror leaves for codex/opencode
.agents/skills/agent-workflow
.opencode/agents/agent-workflow.md
opencode.json
